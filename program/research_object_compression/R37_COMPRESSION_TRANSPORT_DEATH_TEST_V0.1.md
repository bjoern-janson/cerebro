# R37 COMPRESSION TRANSPORT — DEATH TEST V0.1

**Repository:** `bjoern-janson/negative-space-search`  
**Frozen head:** `307c24576ebea951be04d187c61e7428f4f0e184`  
**Frozen root tree:** `6d1aa195fca73eee8c0cb733619357d5b79fdbc3`  
**Effective source surface:** 84 blobs = 82 admitted + 2 explicit exclusions  
**Effective exhaustive parse:** 113 units / 0 unresolved  
**Effective compression:** 113 identity-sized version/standing/provenance-pure items  
**Effective projection:** 113 exact direct primary owners  
**Persistent record state:** `FROZEN`  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

## 1. Terminal accounting

```text
R37_TOTAL_BLOB_PATHS                    = 84
R37_EFFECTIVE_SOURCE_PATHS              = 82
R37_SCOPE_EXCLUDED_PATHS                = 2
R37_UNENUMERATED_PATHS                  = 0

R37_EFFECTIVE_PARSE_UNITS               = 113
R37_PARSED_REPRESENTATIONS              = 113
R37_EXPLICITLY_UNRESOLVED               = 0
R37_PARSER_FAILURES                     = 0

R37_PRIMARY_PROJECTION_ENTRIES          = 113
R37_DIRECT_PROJECTION_ENTRIES           = 113
R37_PRIMARY_COMPRESSED_ITEMS            = 113
R37_UNMAPPED_PARSE_UNITS                = 0
R37_DUPLICATE_PRIMARY_OWNERS            = 0
R37_EXTRA_PROJECTION_UNITS              = 0
R37_SOURCE_PATH_MISMATCHES              = 0
R37_SOURCE_STANDING_MISMATCHES          = 0
```

Canonical projection-pair list SHA-256:

`e6b693614074c8e7e4f3ed0cd936719e2c0db65148062c72498097016f694a24`

## 2. Why compression remains identity-sized

The parse already isolates:

- current v0.7 claims and nonclaims;
- historical v0.1 conceptual status;
- each versioned benchmark contract;
- each preregistration/case matrix;
- each implementation/test surface;
- source-reported historical execution provenance;
- externally verified v0.7 execution provenance;
- persisted result narratives;
- negative-result lineage;
- archival/release metadata.

Further grouping would merge at least one version, standing or provenance class.

```text
LOSS_BOUNDED_COMPRESSION != REQUIRED_ITEM_COUNT_REDUCTION
VERSION_AND_PROVENANCE_PURITY > ARTIFICIAL_COMPRESSION_RATIO
```

## 3. Current claim firewall

The strongest surviving v0.7 claim is narrow:

```text
matched repair/invention quality
+ lower unnecessary representational search
+ lower false expansion
under the frozen synthetic supplied-language benchmark
```

Transport rejects:

```text
SEARCH_GOVERNANCE_AS_SUPERIOR_INVENTION       = CONTAINED
SEARCH_COST_REDUCTION_AS_GENERAL_INTELLIGENCE = CONTAINED
V0_7_AS_AUTONOMOUS_DISCOVERY                  = CONTAINED
V0_7_AS_OPEN_ENDED_INTERFACE_INVENTION        = CONTAINED
```

The source's contraction of earlier stronger claims is part of the scientific record.

## 4. Developmental-version firewall

R37's version ladder is preserved as distinct historical states:

```text
v0.1   hostile observational equivalence / initial causal-absence benchmark
v0.1.1 model-adequacy evidence-selection discriminator
v0.2   false-escalation / history-aware null
v0.3   typed-representation transfer / identity null
v0.4   representation selection and repair from supplied relations
v0.5   basis inadequacy and supplied AND composition
v0.6   construction-language boundary diagnosis
v0.7   conditional generic synthesis inside supplied meta-language
```

Transport preserves:

```text
LATER_VERSION != RETROACTIVE_REWRITE_OF_EARLIER_VERSION
LATER_NARROWER_SUCCESS != RETROACTIVE_PROOF_OF_EARLIER_STRONGER_CLAIM
CLAIM_CONTRACTION != DELETION_OF_NEGATIVE_EVIDENCE
```

## 5. Negative-result preservation

The terminal node retains all five open ledger constraints:

```text
NR_001 = original model-inadequacy/localization failure remains open
NR_002 = no incremental advantage over strong general causal reasoning remains open
NR_003 = history-aware causal-search null remains open
NR_004 = typed-representation identity/uniqueness null remains open
NR_005 = supplied meta-language boundary remains open
```

The governing rule survives compression:

```text
CODE_CHANGE_THAT_MAKES_OLD_CASE_PASS != NEGATIVE_RESULT_CLOSED
```

A new discriminating experiment is required for closure.

## 6. Search-governance result

The frozen v0.7 benchmark reports and the independently fetched CI log support:

```text
BOUNDARY_GATED_REPAIR_SELECTION = 1.0
ALWAYS_EXPAND_REPAIR_SELECTION  = 1.0
BOUNDARY_GATED_MULTI_FAMILY_TRANSFER = 1.0
ALWAYS_EXPAND_MULTI_FAMILY_TRANSFER  = 1.0
BOUNDARY_GATED_REPRESENTATION_COST = 20
ALWAYS_EXPAND_REPRESENTATION_COST  = 20
BOUNDARY_GATED_SEARCH_COST = 5200
ALWAYS_EXPAND_SEARCH_COST  = 7800
BOUNDARY_GATED_FALSE_EXPANSION = 0.0
ALWAYS_EXPAND_FALSE_EXPANSION  = 1.0
```

Therefore the positive discriminator is when/where to invoke the same synthesis mechanism.

```text
WHEN_TO_SEARCH_ADVANTAGE != WHAT_TO_INVENT_ADVANTAGE
```

## 7. Supplied meta-language firewall

The v0.7 generator is finite and source-supplied:

```text
terminals = t0,t1,t2,t3
constructors = ADD,SUB
comparators = GT,LT
numeric expressions = 26
generated predicates = 1300
```

Generated operators include:

```text
GT(t3,t0)
GT(t0,t3)
GT(ADD(t1,t2),ADD(t0,t3))
GT(ADD(t0,t3),ADD(t1,t2))
```

Transport preserves:

```text
GENERIC_ENUMERATIVE_SYNTHESIS != PRIMITIVE_INVENTION
PREDICATE_SYNTHESIS != META_LANGUAGE_INVENTION
GENERATED_OPERATOR != VALIDATED_CAUSAL_CONCEPT
Q_INVENTION_METRIC != OPEN_ENDED_DISCOVERY_AUTHORITY
```

## 8. Boundary-detection firewall

v0.6 demonstrates that hostile classes can share every expression in the current 78-expression language while differing in raw ordered trace.

```text
CURRENT_LANGUAGE_NONIDENTIFIABILITY != RAW_DATA_NONIDENTIFIABILITY
BOUNDARY_DETECTION != INTERFACE_INVENTION
CONSERVATIVE_ABSTENTION != LANGUAGE_FAILURE_LOCALIZATION
ORACLE_RECOVERABILITY != DISCOVERY
```

The expanded oracle is supplied trace-direction operators and is therefore a recoverability control only.

## 9. Representation and basis firewall

Transport preserves three separate stages:

```text
TYPED_RELATIONAL_MEMORY
!= SELECTION_FROM_SUPPLIED_RELATION_LIBRARY
!= COMPOSITION_FROM_SUPPLIED_PRIMITIVES
```

v0.3's typed-general and negative-space policies instantiate the same learner class and produce identical decisions.

v0.4's adaptive learner selects among a frozen generic candidate relation library.

v0.5's gated learner composes supplied primitives with a supplied AND operator.

Thus:

```text
TYPED_GAIN != UNIQUE_NEGATIVE_SPACE_MECHANISM
REPRESENTATION_REPAIR != RELATION_PRIMITIVE_INVENTION
AND_COMPOSITION != PRIMITIVE_OPERATOR_INVENTION
```

## 10. Synthetic-ground-truth firewall

The benchmark supplies synthetic cases, latent labels, correct probes, training/held-out partitions and scoring contracts.

```text
SYNTHETIC_GROUND_TRUTH != EXTERNALLY_IDENTIFIED_CAUSAL_STRUCTURE
BENCHMARK_TARGET != NATURAL_CAUSAL_ORACLE
PERFECT_SYNTHETIC_TRANSFER != EXTERNAL_GENERALIZATION
```

## 11. Preregistration / implementation / execution ladder

R37 contains unusually rich lineage and transport keeps every rung distinct:

```text
BENCHMARK_SPECIFICATION
!= PREREGISTRATION
!= FROZEN_CASE_MATRIX
!= IMPLEMENTATION
!= TEST_SOURCE
!= WORKFLOW_SOURCE
!= WORKFLOW_EXECUTION
!= RESULT_NARRATIVE
!= CLAIM_BOUNDARY
```

The existence of all rungs strengthens provenance but does not collapse their standing.

## 12. Independently verified v0.7 CI execution

R37 ingestion independently retrieved:

```text
RUN_ID = 31177528904
JOB_ID = 92862772195
EVENT = pull_request
TESTED_PR_HEAD = ffe7d4ef75e60eae8da38e0a8f62db03f801a3f0
CONCLUSION = success
PYTHON = 3.11.15
TEST_COUNT = 70
TEST_RESULT = OK
RUNNERS = v0.1 through v0.7
```

The logs contain the v0.7 generated operators and the 5200/7800 candidate-evaluation outputs.

This is a genuine execution record.

It is not independent replication:

```text
REPOSITORY_CI_EXECUTION != INDEPENDENT_REPLICATION
```

## 13. Tested state / frozen release-head firewall

The frozen R37 head is:

`307c24576ebea951be04d187c61e7428f4f0e184`

not the tested PR head.

Direct Git comparison from tested PR head to frozen release head shows later archival/release/result-document changes but no changes to benchmark implementation, synthesis implementation, tests or the CI benchmark workflow.

Therefore:

```text
EXECUTABLE_TESTED_STATE_UNCHANGED_TO_RELEASE = VERIFIED
TESTED_STATE_IDENTITY_WITH_RELEASE_HEAD       = FALSE
```

This supports transport of the execution evidence to the unchanged executable semantics, while preserving distinct Git-state identity.

## 14. CI artifact / archival result firewall

The final v0.7 result/claim/archive documents were added after the verified PR-state execution. Their reported benchmark numbers agree with the fetched CI log.

Transport preserves:

```text
NUMERIC_CONCORDANCE != ARTIFACT_IDENTITY
LATER_RESULT_NARRATIVE != DIRECT_CI_OUTPUT_ARTIFACT
ARCHIVAL_INTERPRETATION != WORKFLOW_LOG
```

## 15. Historical execution-provenance ceiling

Historical result files report CI runs for v0.1 through v0.6, including reported passing test counts. Those run IDs were not independently fetched during this R37 ingestion pass.

Transport therefore keeps:

```text
SOURCE_REPORTED_HISTORICAL_EXECUTION_PROVENANCE
!= EXTERNALLY_RETRIEVED_EXECUTION_RECORD
```

Only v0.7 receives the stronger verified standing.

## 16. Frozen-head execution surface

The queried frozen-head connector surfaces return:

```text
PR_TRIGGERED_WORKFLOW_RUN_WITNESSES_AT_HEAD = 0
COMBINED_STATUS_WITNESSES_AT_HEAD = 0
```

This does not conflict with the earlier verified PR-state run.

```text
NO_HEAD_RUN_WITNESS != UNIVERSAL_HISTORICAL_NONEXECUTION
```

## 17. Test-source / test-execution firewall

All seven test files are internally coherent with the frozen versioned implementations and explicitly test the key hostile properties and null comparisons.

But:

```text
TEST_SOURCE != TEST_EXECUTION
```

The 70-test PR-state log is the execution witness; the files themselves are only test source.

## 18. Release/citation firewall

R37 contains CI source, release source, package metadata, citation metadata and Zenodo-style archive metadata.

```text
RELEASE_AUTOMATION != BENCHMARK_RUN
RELEASE_PUBLICATION != INDEPENDENT_VALIDATION
CITATION_METADATA != SCIENTIFIC_WARRANT
ARCHIVE_METADATA != REPLICATION
```

## 19. Commit chronology

The initial repository commit is:

`f84d755959aa927bd9f1cb2ee1b0962d249cc0cc`

Git compare places the frozen head 120 commits ahead of the initial object, giving 121 reachable commits including the initial commit in that comparison.

The history visibly preserves repeated cycles:

```text
specification/preregistration
-> case freeze
-> implementation
-> hostile tests
-> CI
-> result recording
-> claim contraction
```

Transport uses this as developmental provenance only.

```text
COMMIT_ORDER != SEMANTIC_EDGE
COMMIT_ORDER != CAUSAL_VALIDATION
DEVELOPMENTAL_REPETITION != INDEPENDENT_WARRANT
```

## 20. Relation assertion / map firewall

The repository mentions Correctable Lineage conceptually.

```text
SOURCE_RELATION_ASSERTION
!= ENDPOINT_IDENTITY
!= RESOLVED_EDGE
!= COMPOSITION
!= AUTHORITY
```

No R37 map edge is emitted.

## 21. Warrant-multiplicity firewall

The same result may appear in code, tests, CI logs, result records, README, CLAIMS, RESULTS and release metadata.

Those are different representations/provenance surfaces, not automatically independent scientific warrants.

```text
SOURCE_OCCURRENCE_COUNT != WARRANT_INDEPENDENCE != WARRANT_MULTIPLICITY
```

Default independence remains `NOT_ESTABLISHED`; multiplicity effect remains `NONE` unless separately identified.

## 22. Zero-unresolved preservation

The effective R37 parse has zero unresolved units after full-source and external execution comparison.

That zero is earned because:

- version differences are explicitly chronological;
- old negative results remain open rather than being normalized away;
- historical run IDs have a lower source-reported provenance standing;
- the one independently verified run has a stronger distinct standing;
- tested-state versus release-head difference is resolved by direct Git comparison;
- later result narratives remain separate from CI artifacts;
- synthetic/meta-language limits are explicit.

```text
ZERO_UNRESOLVED != SILENT_NORMALIZATION
```

## 23. Transport attack matrix

```text
SEARCH_GOVERNANCE_AS_SUPERIOR_INVENTION                 = CONTAINED
Q_INVENTION_AS_AUTONOMOUS_DISCOVERY                     = CONTAINED
GENERATED_PREDICATE_AS_CAUSAL_TRUTH                     = CONTAINED
SUPPLIED_META_LANGUAGE_SYNTHESIS_AS_META_LANGUAGE_INVENTION = CONTAINED
LANGUAGE_BOUNDARY_DETECTION_AS_INTERFACE_INVENTION      = CONTAINED
ORACLE_RECOVERABILITY_AS_DISCOVERY                      = CONTAINED
TYPED_REPRESENTATION_GAIN_AS_UNIQUE_NSS_MECHANISM       = CONTAINED
LATER_SUCCESS_AS_NEGATIVE_RESULT_CLOSURE                = CONTAINED
CI_SUCCESS_AS_INDEPENDENT_REPLICATION                   = CONTAINED
TESTED_PR_STATE_AS_FROZEN_HEAD_IDENTITY                 = CONTAINED
LATER_RESULT_NARRATIVE_AS_DIRECT_CI_ARTIFACT            = CONTAINED
SOURCE_REPORTED_HISTORICAL_RUN_AS_EXTERNALLY_VERIFIED   = CONTAINED
WORKFLOW_SOURCE_AS_EXECUTION                             = CONTAINED
RELEASE_WORKFLOW_AS_SCIENTIFIC_EXECUTION                = CONTAINED
RELEASE_METADATA_AS_VALIDATION                          = CONTAINED
SOURCE_RELATION_AS_CEREBRO_EDGE                         = CONTAINED
VERSION_SEQUENCE_AS_MONOTONIC_INTELLIGENCE_LADDER       = CONTAINED
CLAIM_CONTRACTION_AS_RETROACTIVE_DELETION               = CONTAINED
SOURCE_OCCURRENCE_AS_INDEPENDENT_WARRANT                = CONTAINED
```

No transport failure remains.

## 24. Repair locality

```text
SOURCE_SURFACE_REPAIR       = NONE
PARSE_REPAIR                = NONE
COMPRESSION_REPAIR          = NONE
PROJECTION_REPAIR           = NONE
POST_TRANSPORT_REPAIR       = NONE
NEW_EPISTEMIC_DISTINCTION   = NO
NEW_GLOBAL_PARSER_ROLE      = NONE
NEW_COMPRESSION_COORDINATE  = NONE
AMENDMENT_005               = NOT_EARNED
```

## 25. Transportability result

```text
EFFECTIVE_COMPRESSION_CONTRACT_TRANSPORT = SUPPORTED_ON_R01_R37_FROZEN_HEADS
```

This is a bounded historical transport statement only.

## 26. Final R37 verdict

```text
R37_SOURCE_SURFACE                    = FROZEN_COMPLETE_84_BLOB_HEAD
R37_FROZEN_HEAD_COMMIT                = 307c24576ebea951be04d187c61e7428f4f0e184
R37_FROZEN_ROOT_TREE                  = 6d1aa195fca73eee8c0cb733619357d5b79fdbc3
R37_TOTAL_BLOB_PATHS                  = 84
R37_RESEARCH_BEARING_PATHS            = 82
R37_SCOPE_EXCLUDED_PATHS              = 2
R37_UNENUMERATED_PATHS                = 0

R37_EFFECTIVE_EXHAUSTIVE_PARSE_UNITS  = 113
R37_PARSED_REPRESENTATIONS            = 113
R37_UNRESOLVED_SOURCE_UNITS           = 0
R37_PARSER_FAILURES                   = 0

R37_PRIMARY_PROJECTION_ENTRIES        = 113
R37_DIRECT_PROJECTION_ENTRIES         = 113
R37_PRIMARY_COMPRESSED_ITEMS          = 113
R37_UNMAPPED_PARSE_UNITS              = 0
R37_DUPLICATE_PRIMARY_OWNERS          = 0
R37_SOURCE_STANDING_MISMATCHES        = 0

R37_EXECUTABLE_IMPLEMENTATION         = PRESENT
R37_TEST_SOURCE                       = PRESENT
R37_WORKFLOW_SOURCE                   = PRESENT
R37_PERSISTED_RESULT_NARRATIVES       = PRESENT
R37_OPEN_NEGATIVE_RESULT_CONSTRAINTS  = 5
R37_VERIFIED_V0_7_CI_RUN              = 31177528904
R37_VERIFIED_V0_7_CI_JOB              = 92862772195
R37_VERIFIED_TEST_COUNT               = 70
R37_VERIFIED_TEST_RESULT              = OK
R37_VERIFIED_EXECUTABLE_STATE_UNCHANGED_TO_RELEASE = YES
R37_PR_TRIGGERED_HEAD_RUNS_QUERIED    = 0
R37_COMBINED_STATUS_RECORDS_QUERIED   = 0
R37_INITIAL_COMMIT                    = f84d755959aa927bd9f1cb2ee1b0962d249cc0cc
R37_COMMITS_AHEAD_OF_INITIAL          = 120
R37_REACHABLE_COMMIT_COUNT_INCLUDING_INITIAL = 121

R37_SOURCE_SURFACE_REPAIR             = NONE
R37_PARSE_REPAIR                      = NONE
R37_COMPRESSION_REPAIR                = NONE
R37_PROJECTION_REPAIR                 = NONE
R37_POST_TRANSPORT_REPAIR             = NONE
R37_REUSABLE_NODE_STATE               = EARNED
Z37_EFFECTIVE_NODE_STATE              = EARNED
R37_MAP_EDGE_EMISSION                 = NONE
R37_MAP_AUTHORITY                     = NONE
R37_SCIENTIFIC_AUTHORITY              = NONE
PROPAGATE_KERNEL                      = NOT_EARNED
AMENDMENT_005                         = NOT_EARNED
```

`Z37_EFFECTIVE_NODE_STATE` is earned by:

```text
frozen complete 84-blob source surface
+ exhaustive 113-unit standing/version/provenance-separated parse
+ zero unresolved units after explicit version/provenance discrimination
+ full preregistration/implementation/test/result lineage preservation
+ independently retrieved successful v0.7 CI execution record
+ verified executable-state continuity from tested PR state to release head
+ five open negative-result constraints preserved
+ identity-sized 113-item loss-bounded compression
+ exact 113-entry one-owner projection
+ terminal transport containment
```

**TERMINAL TRANSPORT VERDICT: PASS.**

## 27. Sequential boundary

```text
Z01_Z37_REUSABLE_NODE_STATE = EARNED
R38_PROGRAM_PARSE_ACCESS    = NEXT_AUTHORIZED_REPOSITORY
R39_R43_PROGRAM_PARSE_ACCESS = NOT_YET_OPENED
```

This authorization is procedural only and creates no R37 -> R38 semantic relation.