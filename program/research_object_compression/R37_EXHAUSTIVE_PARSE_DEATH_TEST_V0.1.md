# R37 EXHAUSTIVE PARSE — DEATH TEST V0.1

**Repository:** `bjoern-janson/negative-space-search`  
**Frozen head:** `307c24576ebea951be04d187c61e7428f4f0e184`  
**Frozen root tree:** `6d1aa195fca73eee8c0cb733619357d5b79fdbc3`  
**Candidate parse:** 113 units / 82 admitted paths  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

## 1. Mechanical closure

```text
TOTAL_BLOB_PATHS                 = 84
ADMITTED_SOURCE_PATHS            = 82
FROZEN_SCOPE_EXCLUSIONS          = 2
UNENUMERATED_PATHS               = 0
EFFECTIVE_PARSE_UNITS            = 113
SOURCE_PATHS_WITH_PRIMARY_UNIT   = 82
SOURCE_PATHS_WITHOUT_PRIMARY_UNIT= 0
DUPLICATE_UNIT_IDS               = 0
PARSER_FAILURES                  = 0
EXPLICITLY_UNRESOLVED            = 0
```

No coverage/cardinality defect is found.

## 2. Version-state attack

R37 is a developmental sequence, not one timeless mechanism.

```text
V0_1   != V0_1_1
V0_1_1 != V0_2
V0_2   != V0_3
V0_3   != V0_4
V0_4   != V0_5
V0_5   != V0_6
V0_6   != V0_7
```

Transport candidate preserves preregistration, implementation, execution/result and claim-boundary units by version.

```text
LATER_NARROWER_SUCCESS_AS_RETROACTIVE_EARLIER_SUCCESS = CONTAINED
VERSION_SEQUENCE_AS_MONOTONIC_INTELLIGENCE_LADDER      = CONTAINED
CLAIM_CONTRACTION_AS_RETROACTIVE_DELETION              = CONTAINED
```

## 3. Negative-result attack

The five open ledger constraints survive later code/results.

```text
NR_001_ORIGINAL_MODEL_INADEQUACY_FAILURE  = PRESERVED_OPEN
NR_002_NO_INCREMENTAL_CAUSAL_ADVANTAGE    = PRESERVED_OPEN
NR_003_HISTORY_AWARE_CAUSAL_NULL           = PRESERVED_OPEN
NR_004_TYPED_REPRESENTATION_IDENTITY_NULL  = PRESERVED_OPEN
NR_005_SUPPLIED_META_LANGUAGE_BOUNDARY     = PRESERVED_OPEN
```

The repository's closure rule is preserved:

```text
OLD_CODE_NOW_PASSES != NEGATIVE_RESULT_CLOSED
```

Closure requires a new discriminating experiment.

## 4. Search-governance / invention attack

The final v0.7 positive result is bounded to search allocation.

```text
BOUNDARY_GATED_HELD_OUT_REPAIR_SELECTION = 1.0
ALWAYS_EXPAND_HELD_OUT_REPAIR_SELECTION  = 1.0
BOUNDARY_GATED_MULTI_FAMILY_TRANSFER     = 1.0
ALWAYS_EXPAND_MULTI_FAMILY_TRANSFER      = 1.0
BOUNDARY_GATED_REPRESENTATION_COST       = 20
ALWAYS_EXPAND_REPRESENTATION_COST        = 20
BOUNDARY_GATED_SEARCH_COST               = 5200
ALWAYS_EXPAND_SEARCH_COST                = 7800
BOUNDARY_GATED_FALSE_EXPANSION           = 0.0
ALWAYS_EXPAND_FALSE_EXPANSION            = 1.0
```

Therefore:

```text
SEARCH_GOVERNANCE != SUPERIOR_INVENTION_QUALITY
LOWER_CANDIDATE_EVALUATIONS != UNIVERSAL_COMPUTE_EFFICIENCY
Q_INVENTION_METRIC_NAME != AUTONOMOUS_DISCOVERY
```

The metric label `q_invention` is a benchmark-local score for successful use of generated operators on hostile held-out cases; it does not grant unrestricted invention standing.

## 5. Supplied-language firewall

v0.7 synthesis occurs inside a frozen finite meta-language:

```text
TERMINALS    = t0,t1,t2,t3
ARITHMETIC   = ADD,SUB
COMPARATORS  = GT,LT
NUMERIC_EXPRESSIONS = 26
GENERATED_PREDICATES = 1300
```

The exact generated predicates include:

```text
direction_forward -> GT(t3,t0)
direction_reverse -> GT(t0,t3)
center_heavy      -> GT(ADD(t1,t2),ADD(t0,t3))
edge_heavy        -> GT(ADD(t0,t3),ADD(t1,t2))
```

Transport preserves:

```text
PREDICATE_SYNTHESIS_INSIDE_SUPPLIED_GRAMMAR
!= PRIMITIVE_INVENTION
!= GRAMMAR_INVENTION
!= META_LANGUAGE_INVENTION
!= OPEN_ENDED_INTERFACE_INVENTION
```

Generated predicates are discriminative benchmark operators, not validated causal concepts.

## 6. Language-boundary firewall

v0.6 establishes collision under every expression in the frozen 78-expression current language while raw ordered traces differ. The oracle recovers the distinction only after being supplied two trace-direction operators.

```text
CURRENT_LANGUAGE_NONIDENTIFIABILITY != RAW_DATA_NONIDENTIFIABILITY
BOUNDARY_DETECTION                  != OPERATOR_INVENTION
ORACLE_RECOVERABILITY               != DISCOVERY
```

The conservative abstainer and boundary-aware auditor remain distinct: avoiding a guess does not by itself localize language failure.

## 7. Representation/basis firewall

v0.3-v0.5 preserve three different claims:

```text
TYPED_HISTORY_TRANSFER
!= ADAPTIVE_SELECTION_FROM_SUPPLIED_RELATION_LIBRARY
!= CONDITIONAL_COMPOSITION_FROM_SUPPLIED_PRIMITIVES
```

Specifically:

- v0.3 typed-general and negative-space policies use the same learner class and produce identical decisions;
- v0.4 selects/repairs among supplied generic relations;
- v0.5 composes supplied relations with a supplied AND operator after basis inadequacy.

Transport rejects:

```text
REPRESENTATION_GAIN_AS_NEGATIVE_SPACE_UNIQUENESS = CONTAINED
RELATION_SELECTION_AS_RELATION_PRIMITIVE_INVENTION = CONTAINED
AND_COMPOSITION_AS_OPERATOR_INVENTION = CONTAINED
```

## 8. Synthetic-ground-truth firewall

The environments/case matrices use source-authored hidden labels, expected probes and synthetic outcomes.

```text
SYNTHETIC_GROUND_TRUTH != EXTERNALLY_IDENTIFIED_CAUSAL_STRUCTURE
BENCHMARK_CORRECT_PROBE != UNIVERSAL_CAUSAL_ORACLE
PERFECT_HELD_OUT_RATE != REAL_WORLD_GENERALIZATION
```

## 9. Preregistration / implementation / result firewall

R37 contains all of these surfaces, and the parse keeps them separate:

```text
PREREGISTRATION
!= FROZEN_CASE_MATRIX
!= IMPLEMENTATION
!= TEST_SOURCE
!= WORKFLOW_SOURCE
!= WORKFLOW_EXECUTION
!= RESULT_NARRATIVE
!= CLAIM_BOUNDARY
```

The staged commit history supports the developmental order but does not by itself validate scientific conclusions.

## 10. Verified CI lineage

R37 ingestion independently retrieved GitHub Actions run `31177528904` and job `92862772195` for the v0.7 PR-state validation.

Verified execution facts include:

```text
CONCLUSION = success
PYTHON = 3.11.15
TESTS = 70
TEST_RESULT = OK
RUNNERS_EXECUTED = v0.1 through v0.7
```

The log also independently contains the reported v0.7 search-cost and generated-operator outputs.

However, the tested source state is not the frozen archival head:

```text
TESTED_PR_HEAD = ffe7d4ef75e60eae8da38e0a8f62db03f801a3f0
FROZEN_R37_HEAD = 307c24576ebea951be04d187c61e7428f4f0e184
```

Direct comparison shows later archival/release/result-document changes, while benchmark implementation, tests, synthesis implementation and CI workflow remain unchanged.

Thus:

```text
VERIFIED_CI_SUPPORTS_UNCHANGED_EXECUTABLE_STATE = YES
VERIFIED_CI_RUN_AT_FROZEN_RELEASE_HEAD           = NO
```

## 11. Result-narrative / CI-artifact firewall

`results/v0.7-operator-discovery.md` and final archival summaries were added after the validated PR-state run, although their numeric benchmark claims match the independently fetched CI output.

```text
LATER_RESULT_NARRATIVE != DIRECT_CI_ARTIFACT
NUMERIC_CONCORDANCE != ARTIFACT_IDENTITY
```

The narrative has execution support but remains a later interpretation/archive object.

## 12. Historical execution-provenance ceiling

Result files report prior run identifiers through v0.1-v0.6. Those identifiers are preserved as source-reported execution provenance. They were not independently fetched in this ingestion pass.

```text
SOURCE_REPORTED_HISTORICAL_RUN_ID != INDEPENDENTLY_RETRIEVED_RUN_RECORD
```

Only the v0.7 run receives the stronger externally verified execution standing in the primary parse.

## 13. Frozen-head Actions/status ceiling

Queried frozen-head connector surfaces return:

```text
PR_TRIGGERED_WORKFLOW_RUNS_AT_R37_HEAD = 0
COMBINED_STATUS_RECORDS_AT_R37_HEAD     = 0
```

This is compatible with the separately verified earlier PR-state run.

```text
NO_HEAD_WITNESS != UNIVERSAL_NONEXECUTION
WORKFLOW_SOURCE != WORKFLOW_RUN
```

## 14. Release/citation firewall

R37 contains a release workflow, package metadata, citation metadata and Zenodo-style archive metadata.

```text
RELEASE_WORKFLOW != BENCHMARK_EXECUTION
RELEASE_METADATA != SCIENTIFIC_VALIDATION
CITATION_METADATA != INDEPENDENT_WARRANT
PACKAGE_TYPE_SOFTWARE != EMPIRICAL_RESULT
```

## 15. Chronology firewall

The initial commit is `f84d755959aa927bd9f1cb2ee1b0962d249cc0cc`. Git compare places R37 head 120 commits ahead of that initial commit, yielding 121 reachable commits including the initial object in this comparison.

The visible history contains repeated cycles of specification/preregistration, case freeze, implementation, tests/CI, result recording and claim contraction.

```text
COMMIT_ORDER != CAUSAL_VALIDATION
CHRONOLOGICAL_ADJACENCY != SEMANTIC_EDGE
DEVELOPMENTAL_SEQUENCE != WARRANT_MULTIPLICITY
```

## 16. Conceptual-lineage firewall

`docs/framework.md` mentions Correctable Lineage as a conceptual relation/scope statement.

```text
SOURCE_RELATION_ASSERTION
!= ENDPOINT_IDENTITY
!= RESOLVED_CEREBRO_EDGE
!= COMPOSITION
!= AUTHORITY
```

No R37 synapse is emitted.

## 17. Source-occurrence multiplicity

Claims and results recur across README, CLAIMS, METHOD, RESULTS, result files, tests, code and release metadata.

```text
SOURCE_OCCURRENCE_MULTIPLICITY
!= WARRANT_INDEPENDENCE
!= WARRANT_MULTIPLICITY
```

Repeated representations may improve reconstructibility without increasing independent scientific warrant.

## 18. Zero-unresolved audit

No unresolved source contradiction is required after full-source comparison:

- version differences are resolved by explicit version chronology;
- claim narrowing is explicit rather than contradictory;
- CI-state versus release-head difference is resolved by Git comparison and separate standing;
- source-reported historical run IDs are expressible with a lower provenance standing rather than guessed verification;
- later result narratives are separated from direct CI artifacts;
- negative results remain open by explicit ledger rule.

Therefore:

```text
EXPLICITLY_UNRESOLVED = 0
ZERO_UNRESOLVED != SILENT_NORMALIZATION
```

## 19. Verdict

```text
SOURCE_COVERAGE                    = PASS
STANDING_SEPARATION                = PASS
VERSION_LINEAGE                    = PASS
NEGATIVE_RESULT_PRESERVATION       = PASS
PREREGISTRATION_EXECUTION_SEPARATION = PASS
CI_LINEAGE                         = PASS
SUPPLIED_LANGUAGE_FIREWALL         = PASS
SEARCH_GOVERNANCE_FIREWALL         = PASS
SOURCE_RELATION_FIREWALL           = PASS
PARSE_REPAIR_REQUIRED              = NO
NEW_EPISTEMIC_DISTINCTION_REQUIRED = NO
NEW_GLOBAL_PARSER_ROLE             = NONE
AMENDMENT_005                      = NOT_EARNED
```

**PARSE DEATH TEST: PASS.**

Compression may open on the unchanged 113-unit effective parse.