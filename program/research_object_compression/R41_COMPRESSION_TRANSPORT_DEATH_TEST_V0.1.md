# R41 Compression Transport Death Test v0.1

## Object
`R41_COMPRESSION_TRANSPORT_DEATH_TEST_V0.1`

Repository: `bjoern-janson/tevpp`  
Frozen research head: `6b38882224bf8d3cd11b237eb6d3caf0718accdc`  
Frozen root tree: `8042216be720d61347bd6276dcd9afc736da95c5`

Terminal state: `PASS`  
Map authority: `NONE`  
Scientific authority added by Cerebro: `NONE`  
Propagation: `NOT_EARNED`

## Transport accounting

```text
R41_USER_RESEARCH_DELTA_PATHS        = 7
R41_AUX_EXEC_CONTROL_PATHS           = 1
R41_EXTERNAL_EXEC_RECORD_SURFACES    = 2
R41_EFFECTIVE_PARSE_UNITS            = 49
R41_PRIMARY_COMPRESSION_ITEMS        = 25
R41_DIRECT_PROJECTION_ENTRIES        = 15
R41_GROUP_PROJECTION_ENTRIES         = 34
R41_DIRECT_SINGLETON_ITEMS           = 15
R41_LOSS_BOUNDED_GROUP_ITEMS         = 10
R41_UNMAPPED_PARSE_UNITS             = 0
R41_DUPLICATE_PRIMARY_OWNERS         = 0
R41_EXTRA_PROJECTION_UNITS           = 0
R41_PATH_MISMATCHES                  = 0
R41_STANDING_MISMATCHES              = 0
R41_STANDING_MIXED_ITEMS             = 0
```

Canonical parse unit-set SHA-256:

`a8ba15fc909e7eca6dce26bad4391ec53f46d60e368a8d44e2b1eec74cf185b6`

Canonical compression item-set SHA-256:

`e2c036a90264fd1524a4353e8ae09e5fe6074e1ba975b953a4668a7176432cee`

Canonical owner-map SHA-256:

`168f8b39abef1dd54815dfd0b68574428fd4ae598bb3550fc5ecb3473a274337`

## Compression contract

Compression is:

`source-local × source-standing-pure`

No item crosses a source standing. Each parse unit has exactly one primary owner. Repeated semantic statements and multiple CI jobs retain:

```text
WARRANT_INDEPENDENCE_STATUS = NOT_ESTABLISHED
WARRANT_MULTIPLICITY_EFFECT = NONE
```

## High-risk transport attacks

| Attack | Transport result |
|---|---|
| Imported-fork ancestry compressed as user research origin | CONTAINED |
| Default branch compressed as audited research head | CONTAINED |
| Later branch repairs compressed into historical R41 state | CONTAINED |
| `runtimeId` compressed as source provenance | CONTAINED |
| Live `MeasurementLineage` compressed as stored result | CONTAINED |
| Return traversal compressed as reconstruction | CONTAINED |
| Two correspondence operators compressed into one | CONTAINED |
| Source invariant compressed as universal empirical law | CONTAINED |
| Metric/formula/configuration compressed as validation | CONTAINED |
| UI implementation compressed as new comparison science | CONTAINED |
| Test source/configuration compressed as execution record | CONTAINED |
| CI execution record compressed as scientific validation | CONTAINED |
| Eight successful jobs compressed as independent warrant multiplicity | CONTAINED |
| Build-graph inference compressed as direct stdout witness | CONTAINED |
| Source lineage names compressed as resolved endpoints | CONTAINED |
| Chronological or branch adjacency compressed as semantic edge | CONTAINED |
| Repeated tokens compressed as global formal identity | CONTAINED |
| Compression topology emitted as Cerebro map edges | CONTAINED |

## R41 source-local state retained

The source describes a deliberately small inspectable-measurement fork:

```text
MeasurementSpec -> MeasurementStage -> ViewSpec
Inputs -> Signed Difference -> Metric Output
```

It preserves:

- candidate/reference and measurement settings separately from presentation-only view state;
- separate interactive and materialized correspondence transforms;
- explicit return traversal across retained live stages;
- a semantic-anchor fixture for correspondence divergence, round-trip traversal, and view isolation;
- an inherited CI matrix whose default build graph includes the semantic-anchor executable.

Ceilings:

```text
SOURCE_ARCHITECTURE != CEREBRO_ONTOLOGY
RUNTIME_ID != SOURCE_PROVENANCE
LIVE_LINEAGE != STORED_RESULT
FIXTURE_SUCCESS != BROAD_EMPIRICAL_VALIDITY
CI_JOB_MULTIPLICITY != WARRANT_INDEPENDENCE
SOURCE_REFERENCE != ENDPOINT_IDENTITY != MAP_EDGE
```

## Execution provenance ceiling

The exact-head GitHub Actions run `31630457443` is `completed/success`. Eight retrieved jobs across Linux, macOS, and Windows are successful, and every retrieved `Build` step is successful. The frozen CMake graph defines an `ALL` target that executes the semantic-anchor binary.

The transport retains these as configuration plus execution records. Direct decoded semantic-anchor stdout was not retrieved, so the compression does not manufacture a stronger direct-output witness.

```text
TEST_SOURCE != CONFIGURATION != EXECUTION_RECORD != BROAD_SCIENTIFIC_VALIDATION
```

## Projection invariants

The exact projection ledger materializes:

```text
49 parse units -> 25 compression items
15 DIRECT_TO_COMPRESSION_ITEM units
34 LOSS_BOUNDED_GROUP_TO_COMPRESSION_ITEM units
```

with:

```text
UNMAPPED_PARSE_UNITS       = 0
DUPLICATE_PRIMARY_OWNERS   = 0
EXTRA_PROJECTION_UNITS     = 0
PATH_MISMATCHES            = 0
STANDING_MISMATCHES        = 0
STANDING_MIXED_ITEMS       = 0
```

## Repair record

```text
SOURCE_SURFACE_REPAIR                 = NONE
PARSE_REPAIR                          = NONE
COMPRESSION_REPAIR                    = NONE
PROJECTION_REPAIR                     = NONE
POST_TRANSPORT_REPAIR                 = NONE
SEMANTIC_UNIT_CHANGE                  = NONE
STANDING_CHANGE                       = NONE
NEW_EPISTEMIC_DISTINCTION_REQUIRED    = NO
NEW_GLOBAL_PARSER_ROLE                = NONE
NEW_TOP_LEVEL_COMPRESSION_COORDINATE  = NONE
AMENDMENT_005                         = NOT_EARNED
```

## Effective transport contract

`EFFECTIVE_COMPRESSION_CONTRACT_TRANSPORT = SUPPORTED_ON_R01_R41_FROZEN_HEADS`

This is a bounded historical transport statement only. It does not authorize endpoint resolution, relation composition, synapse construction, propagation, or scientific authority.

## Terminal node state

```text
R41_REUSABLE_NODE_STATE   = EARNED
Z41_EFFECTIVE_NODE_STATE  = EARNED
R41_MAP_EDGE_EMISSION     = NONE
R41_MAP_AUTHORITY         = NONE
R41_SCIENTIFIC_AUTHORITY  = NONE
PROPAGATE_KERNEL          = NOT_EARNED
AMENDMENT_005             = NOT_EARNED
```

Sequential state:

```text
Z01_Z41_REUSABLE_NODE_STATE = EARNED
R42_PROGRAM_PARSE_ACCESS    = NEXT_AUTHORIZED_REPOSITORY
R43_PROGRAM_PARSE_ACCESS    = NOT_YET_OPENED
```

## Neural firewall

`E_map=0`

`Propagate=NOT_EARNED`

`Authority=0`

## Terminal verdict

`R41_COMPRESSION_TRANSPORT_DEATH_TEST_V0.1 = PASS`

R41 becomes the forty-first earned Cerebro research neuron only after this terminal record is persisted and read back from `main`.

R42 is procedurally next-authorized only. It is not opened by this artifact.
