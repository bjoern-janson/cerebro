# R18 EXHAUSTIVE PARSE — DEATH TEST V0.1

**Candidate:** `R18_EXHAUSTIVE_PARSE_V0.1.json` + Part A  
**Persistent record state:** `FROZEN`  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

R18 tests whether a one-file theory/experiment-design repository can be exhaustively parsed without promoting theoretical regime language to empirical result standing, without treating an experiment specification as execution, and without silently operationalizing formal symbols that the source leaves underspecified.

## 1. Candidate accounting

```text
R18_SOURCE_PATHS                    = 1
R18_CANDIDATE_PARSE_UNITS           = 75
R18_PARSED_REPRESENTATIONS          = 60
R18_UNRESOLVED_SOURCE_UNITS         = 15
R18_PARSER_FAILURES                 = 0
```

## 2. Attack matrix

```text
THEORY_ASSERTION_AS_RESULT                    = CONTAINED
PHASE_REGIME_DESCRIPTION_AS_OBSERVATION       = CONTAINED
EMPIRICAL_OBSERVABLE_LABEL_AS_FULL_PROTOCOL   = CONTAINED
APE_SPECIFICATION_AS_EXECUTION                = CONTAINED
FALSIFICATION_CRITERION_AS_FALSIFICATION      = CONTAINED
PREDICTION_AS_MEASURED_ORDERING               = CONTAINED
UNDEFINED_SYMBOL_AS_CANONICALIZED_DEFINITION  = CONTAINED
SOURCE_UNDERSPECIFICATION_AS_PARSER_FAILURE   = CONTAINED
A_T_CAPITAL_AS_AGENT_A                        = CONTAINED
LAMBDA_T_INVARIANT_AS_lambda_OBSOLESCENCE     = CONTAINED
CURRENT_HEAD_AS_COMPLETE_HISTORY              = CONTAINED
REMOVED_INTERMEDIATE_WORDING_AS_CURRENT_UNIT  = CONTAINED
REPOSITORY_LEVEL_ABSENCE_AS_FILE_SOURCE_UNIT  = CONTAINED
CROSS_REPOSITORY_VOCABULARY_AS_IDENTITY_EDGE  = CONTAINED
```

## 3. Contained — theory/result standing

The current README states a theory, invariant, phase regimes and consequences. None is represented as a reported empirical result.

In particular:

```text
Lambda > 1 -> increased resilience
Lambda = 1 -> survival without adaptive margin
Lambda < 1 -> temporary reserve-supported function / structural debt
```

remain source hypotheses/conceptual consequences.

```text
THEORY_ASSERTION_AS_RESULT              = CONTAINED
PHASE_REGIME_DESCRIPTION_AS_OBSERVATION = CONTAINED
```

## 4. Contained — empirical-observable versus operational protocol

The source calls Psi an empirical observable, but the parse does not invent measurement procedures for kappa or tau_a. Those missing operational details remain explicit unresolved source fragments.

```text
EMPIRICAL_OBSERVABLE_LABEL_AS_FULL_PROTOCOL = CONTAINED
SOURCE_UNDERSPECIFICATION_AS_PARSER_FAILURE = CONTAINED
```

Likewise Lambda's comparison to 1 is preserved while normalization/commensurability of dot(K_eff) and v_req remains unresolved rather than silently repaired.

## 5. Contained — APE specification, prediction and falsification standing

APE is represented as a methodology/test protocol with source predictions:

```text
Agent A -> Psi_A approximately 0
Agent B -> Psi_B > 0
Agent C -> v_rev up, kappa down, Psi_C < 0
future latency -> tau_B < tau_A < tau_C
```

The source contains no execution record or reported measurement.

The falsification sentence is a criterion for what would falsify the theory, not evidence that falsification occurred.

```text
APE_SPECIFICATION_AS_EXECUTION           = CONTAINED
FALSIFICATION_CRITERION_AS_FALSIFICATION = CONTAINED
PREDICTION_AS_MEASURED_ORDERING          = CONTAINED
```

## 6. Contained — local namespace separability

The parse keeps these distinct:

```text
A_t      = adaptive-capital stock
Agent A  = standard learner comparator
Lambda_t = adaptive invariant
lambda   = adaptive-capital obsolescence parameter
```

No same-token or visually similar-token identity is inferred.

```text
A_T_CAPITAL_AS_AGENT_A                    = CONTAINED
LAMBDA_T_INVARIANT_AS_lambda_OBSOLESCENCE = CONTAINED
```

## 7. Contained — current head versus developmental provenance

The repository history is reconstructible as:

```text
initial two-line title/subtitle
-> full theory introduced
-> expanded/reformatted intermediate version
-> current condensed/reformatted head
```

Intermediate wording removed by the current commit is not admitted into the current-head primary parse.

```text
CURRENT_HEAD_AS_COMPLETE_HISTORY             = CONTAINED
REMOVED_INTERMEDIATE_WORDING_AS_CURRENT_UNIT = CONTAINED
```

The history remains secondary developmental provenance, not extra current source units.

## 8. Contained — source versus aperture audit

Repository-level observations such as:

```text
no executable source on frozen head
no dataset on frozen head
no persisted execution artifact on frozen head
no reported-result document on frozen head
```

are supported by the exhaustive source inventory but are not file-local README propositions. They therefore remain source-surface/audit facts rather than primary parse units.

```text
REPOSITORY_LEVEL_ABSENCE_AS_FILE_SOURCE_UNIT = CONTAINED
```

## 9. Contained — no cross-repository synthesis

Terms such as permeability, revision capacity, adaptation, reality coupling and intelligence may resemble prior repository vocabulary. R18 parsing performs no endpoint identity, lineage, equivalence, support or successor inference.

```text
CROSS_REPOSITORY_VOCABULARY_AS_IDENTITY_EDGE = CONTAINED
MAP_EDGE_EMISSION                             = NONE
```

## 10. Verdict

No demonstrated R18 parse failure requires a new parser role or contract amendment.

```text
PARSE_REPAIR_REQUIRED                = NO
NEW_EPISTEMIC_DISTINCTION_REQUIRED  = NO
NEW_GLOBAL_PARSER_ROLE               = NONE
NEW_TOP_LEVEL_COMPRESSION_COORDINATE = NONE
AMENDMENT_005                        = NOT_EARNED
```

Effective parse:

```text
R18_EFFECTIVE_EXHAUSTIVE_PARSE_UNITS = 75
R18_PARSED_REPRESENTATIONS           = 60
R18_UNRESOLVED_SOURCE_UNITS          = 15
R18_PARSER_FAILURES                  = 0
R18_EXHAUSTIVE_PARSE_STATE           = EARNED_FOR_COMPRESSION_INPUT
```

This authorizes only the R18 compression step. It creates no Program Map relation and grants no scientific authority.
