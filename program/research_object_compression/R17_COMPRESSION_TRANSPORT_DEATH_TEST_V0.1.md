# R17 COMPRESSION TRANSPORT — DEATH TEST V0.1

**Candidate:** `R17_COMPRESSION_V0.1.json`  
**Projection:** `R17_PARSE_TO_COMPRESSION_PROJECTION_LEDGER_V0.1.json` + Parts A-B  
**Persistent record state:** `FROZEN`  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

R17 tests whether a 231-unit two-surface causal-framework parse can be transported into a reusable neuron without semantic-identity collapse, standing promotion, mapped-unit loss, or authority leakage.

## 1. Projection accounting

```text
R17_EFFECTIVE_PRIMARY_PARSE_UNITS = 231
R17_PRIMARY_PROJECTION_ENTRIES    = 231
R17_PRIMARY_COMPRESSED_ITEMS      = 88
R17_UNMAPPED_PARSE_UNITS          = 0
R17_DUPLICATE_PRIMARY_OWNERS      = 0
R17_UNRESOLVED_SOURCE_UNITS       = 21
R17_DERIVED_AUDIT_VIEWS           = 3
R17_DERIVED_BOUNDED_ABSENCES      = 1
```

## 2. Attack matrix

```text
FRAMEWORK_OVERLAP_AS_IDENTITY_COLLAPSE            = CONTAINED
EXPERIMENT_SPEC_AS_REPORTED_RESULT                = CONTAINED
PREDICTION_AS_EXECUTION                           = CONTAINED
CAUSAL_ASSERTION_AS_EMPIRICAL_RESULT              = CONTAINED
REPOSITORY_ABSENCE_AS_PRIMARY_SOURCE_CONTENT      = CONTAINED
RECURRENCE_AS_INDEPENDENT_WARRANT                 = CONTAINED
TEMPORAL_LATEST_STATE_AS_RETROACTIVE_CLEANUP      = CONTAINED
SOURCE_LINEAGE_AS_PROGRAM_MAP_EDGE                = CONTAINED
UNRESOLVED_SYMBOL_AS_CANONICALIZED_DEFINITION     = CONTAINED
PRIMARY_ITEM_STANDING_LEAK                        = HIT
MAPPED_DISTINCTION_DROPPED_BY_GROUPING            = HIT
```

## 3. Contained — framework identity

Meta-Process Framework and RAD remain separately owned primary items. Their similarity is represented only by the zero-authority audit view:

```text
R17:AUDIT:FRAMEWORK_IDENTITY_EQUIVALENCE
```

No equivalence, successor, or Program Map edge is emitted.

```text
FRAMEWORK_OVERLAP_AS_IDENTITY_COLLAPSE = CONTAINED
```

## 4. Contained — experimental standing

CTC remains question/formalization/interpretation/limitation. RVT001 remains question/method/formalization. RAD simulator remains purpose/formalization/hypothesis/prediction.

The candidate contains no primary `REPORTED_EMPIRICAL_RESULT`, `REPORTED_NEGATIVE_RESULT`, `EXECUTABLE_IMPLEMENTATION`, or `EXECUTION_RECORD` standing.

```text
EXPERIMENT_SPEC_AS_REPORTED_RESULT   = CONTAINED
PREDICTION_AS_EXECUTION              = CONTAINED
CAUSAL_ASSERTION_AS_EMPIRICAL_RESULT = CONTAINED
```

The exhaustive-head absence of implementation/execution/result artifacts remains only:

```text
R17:ABSENCE:EXECUTION_AND_RESULT_ARTIFACTS
AUTHORITY_EFFECT = NONE
```

## 5. Contained — recurrence, history and lineage

Repeated causal-access propositions across the two source files retain source occurrence through the projection ledger but do not create warrant multiplicity.

The four-commit developmental history remains a provenance view distinct from the current-head parse.

The unresolved RAD parent-research-program reference creates no endpoint resolution or map edge.

```text
RECURRENCE_AS_INDEPENDENT_WARRANT            = CONTAINED
TEMPORAL_LATEST_STATE_AS_RETROACTIVE_CLEANUP = CONTAINED
SOURCE_LINEAGE_AS_PROGRAM_MAP_EDGE           = CONTAINED
```

## 6. Contained — unresolved symbols

All 21 unresolved source fragments project to the two unresolved primary compression items. No undefined symbol is filled from another section or source merely because a token resembles a defined token elsewhere.

```text
UNRESOLVED_SYMBOL_AS_CANONICALIZED_DEFINITION = CONTAINED
```

## 7. HIT — primary standing leak in generator-improvement compression

The candidate item:

```text
R17:FORMAL:RAD_GENERATOR_IMPROVEMENT
standing = FORMAL_STRUCTURE
```

is primarily owned by `R17:P:B048`, the formal transition `G_m -> Gbar_m`, but its compressed text also says `Normal/evolutionary/higher-order improvement forms`, thereby reusing definition-level content from B046/B047 without typed secondary provenance.

```text
PRIMARY_ITEM_STANDING_LEAK = HIT
```

Required repair: restrict the formal item to the B048 formal transition and keep normal/evolutionary/higher-order definitions in `R17:DEF:RAD_GENERATOR_IMPROVEMENT`.

## 8. HIT — mapped distinctions under-described by loss-bounded grouping

Three candidate summaries are too compressed relative to their explicit ownership sets:

```text
R17:ASSERT:META_CAUSAL_ACCESS
R17:ASSERT:RAD_CORE
R17:DEF:RAD_GENERATOR_IMPROVEMENT
```

The projection ledger is complete, but a complete ledger does not excuse a destination summary that fails to preserve distinctions it claims to own.

Missing/under-described distinctions include:

```text
trajectory moves through a space
open recursion lets reality participate in changing future rules
open-endedness is not mere change
RAD target is not merely whether the system adapts
normal improvement = C increases
evolutionary improvement = G_m increases
higher-order improvement improves the machinery that improves mechanisms
```

```text
MAPPED_DISTINCTION_DROPPED_BY_GROUPING = HIT
```

## 9. Verdict

Both defects are local compression-content defects. Neither requires a new parser role, a new compression coordinate, or a new global ontology.

```text
NEW_EPISTEMIC_DISTINCTION_REQUIRED  = NO
NEW_GLOBAL_PARSER_ROLE               = NONE
NEW_TOP_LEVEL_COMPRESSION_COORDINATE = NONE
NEW_COMPRESSION_CONTRACT_AMENDMENT   = NOT_EARNED
LOCAL_COMPRESSION_REPAIR_REQUIRED    = YES
PRIMARY_PROJECTION_REMAP_REQUIRED    = NO
```

Required repair:

1. replace the four affected primary-item contents without changing their IDs/coordinates/standings;
2. preserve all 231 existing primary projection owners;
3. rerun transport attacks after the overlay.

Until that repair passes:

```text
R17_REUSABLE_NODE_STATE = NOT_YET_EARNED
R18_PROGRAM_PARSE_ACCESS = NOT_YET_OPENED
MAP_EDGE_EMISSION = NONE
PROPAGATE_KERNEL = NOT_EARNED
```
