# R22 COMPRESSION TRANSPORT — DEATH TEST V0.1

**Repository:** `bjoern-janson/causal-transition-condition`  
**Frozen head:** `fe899838979fcf0e6ee2f43011420a79b58daff8`  
**Effective parse:** 278 units after parse Amendment 001/retest  
**Compression:** `R22_COMPRESSION_V0.1.json` + Parts A–D  
**Projection ledger:** `R22_PARSE_TO_COMPRESSION_PROJECTION_LEDGER_V0.1.json`  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

## 1. Derivation accounting

```text
R22_FROZEN_HEAD_PATHS              = 24
R22_EFFECTIVE_PARSE_UNITS          = 278
R22_PARSED_REPRESENTATIONS         = 206
R22_EXPLICITLY_UNRESOLVED          = 72
R22_PRIMARY_PROJECTION_ENTRIES     = 278
R22_PRIMARY_COMPRESSED_ITEMS       = 120
R22_UNMAPPED_PARSE_UNITS           = 0
R22_DUPLICATE_PRIMARY_OWNERS       = 0
R22_EXTRA_PROJECTION_UNITS         = 0
```

## 2. Attack matrix

```text
NOMENCLATURE_VARIANTS_AS_SINGLE_IDENTITY                = CONTAINED
CAUSAL_PERMEABILITY_AS_R18_ENDPOINT                     = CONTAINED
SOURCE_RELATION_AS_PROGRAM_EDGE                         = CONTAINED
MATHCAL_G_PROXY_AS_VALIDATED_MECHANISM_SPACE            = CONTAINED
OMEGA_TO_DELTA_G_ARROW_AS_CAUSAL_IDENTIFICATION         = CONTAINED
WEAK_CTC_AS_STRONG_CTC                                  = CONTAINED
TREATMENT_SWITCH_AS_MEASURED_PERMEABILITY               = CONTAINED
TREATMENT_CODED_MECHANISM_GROWTH_AS_SCIENTIFIC_RESULT   = CONTAINED
SYNTHETIC_REMOVAL_AS_MEASURED_RESIDUAL_CAPABILITY      = CONTAINED
PLACEHOLDER_EXPORT_DATA_AS_RESULT                       = CONTAINED
PLACEHOLDER_PLOT_DATA_AS_RESULT                         = CONTAINED
RUNTIME_SUPPORTED_LABEL_AS_REPORTED_RESULT              = CONTAINED
SOURCE_INTERFACE_DEFECT_AS_FAILED_EXECUTION             = CONTAINED
TEST_SOURCE_AS_TEST_RUN                                 = CONTAINED
GITIGNORE_RESULT_ABSENCE_AS_NO_EXECUTION                = CONTAINED
CONCEPTUAL_RESULT_LABEL_AS_EMPIRICAL_RESULT             = CONTAINED
SOURCE_RECURRENCE_AS_INDEPENDENT_WARRANT                = CONTAINED
PROJECT_METADATA_AS_NEW_TOP_LEVEL_COMPRESSION_COORDINATE = HIT
CURRENT_HEAD_AS_DEVELOPMENTAL_HISTORY                   = CONTAINED
```

One local compression-coordinate hit is found.

## 3. Local compression hit: project metadata coordinate

`R22:C090` preserves `.gitignore` project/process metadata. Its candidate top-level coordinate is:

```text
PROJECT_PROCESS_METADATA
```

The information is worth retaining, especially because ignore policy changes the interpretation of absent result artifacts. But a new top-level compression coordinate is unnecessary.

The existing source-status channel can carry the same information while preserving the item's standing:

```text
coordinate:
PROJECT_PROCESS_METADATA
->
SOURCE_DESCRIBED_STATUS

standing:
PROJECT_PROCESS_METADATA
(unchanged)
```

This is a local compression repair, not evidence that the global compression ontology is inadequate.

```text
NEW_EPISTEMIC_DISTINCTION_REQUIRED   = NO
NEW_TOP_LEVEL_COMPRESSION_COORDINATE = NO
AMENDMENT_005                        = NOT_EARNED
```

The 278-unit projection ownership remains unchanged because `SOURCE_UNITS` for `R22:C090` do not change.

## 4. Causal/construct ceilings survive compression

The candidate correctly preserves the central difference between a scientific CTC claim and the toy instrument implementing it.

The current prototype contains:

```text
permeability_score = 1 iff treatment switch is active
mechanism generation = enabled only by treatment switch
mechanism identity = string adapt_to_<constraint.source>
```

Those are implementation facts. They do not by themselves establish:

```text
E* causally changes a general adaptive-mechanism space
mathcal G has expanded in the scientific sense
open-ended adaptation has increased
```

The construct-validity audit remains zero-authority.

## 5. Execution ladder remains intact

R22 contains code, runners and tests, but the frozen head has no CI/status/test/benchmark result artifact.

Moreover `.gitignore` excludes ordinary generated result artifacts.

Therefore:

```text
implementation present
!= execution established
!= test pass/failure established
!= benchmark result established
```

The many source-level API mismatches are implementation/interface facts, not observed failed test events.

## 6. Conceptual source-reported result remains limited

The evolution log's Stage 6 `Result:` survives as a source conceptual assertion after the parse standing repair.

It remains distinct from:

```text
empirical result
execution record
independent source
replication
scientific authority
```

## 7. Nomenclature and endpoint discipline

Compression preserves local labels:

```text
Causal Transition Condition
Constraint Transition Condition
Constraint–Trajectory Coupling
Causal Permeability Principle
```

without assuming global identity.

It also preserves explicit source relations to Causal Permeability but does not resolve that endpoint to R18 or any other corpus node.

```text
RELATION_ASSERTION_PRESENT = YES
ENDPOINT_IDENTITY_RESOLVED = NO
PROGRAM_EDGE_EMITTED       = NO
```

## 8. Death-test verdict

```text
R22_PRIMARY_COMPRESSED_ITEMS           = 120
R22_LOCAL_COMPRESSION_REPAIR_REQUIRED  = 1 COORDINATE_ONLY
R22_PROJECTION_REMAPS_REQUIRED         = 0
NEW_EPISTEMIC_DISTINCTION_REQUIRED     = NO
NEW_GLOBAL_PARSER_ROLE                 = NONE
NEW_TOP_LEVEL_COMPRESSION_COORDINATE   = NONE
AMENDMENT_005                          = NOT_EARNED
```

`Z22` is blocked until the one-item compression overlay is frozen and retested.
