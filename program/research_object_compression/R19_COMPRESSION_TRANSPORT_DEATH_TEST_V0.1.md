# R19 COMPRESSION TRANSPORT — DEATH TEST V0.1

**Compression candidate:** `R19_COMPRESSION_V0.1.json`  
**Effective parse before this death test:** 345 units  
**Projection ledger:** `R19_PARSE_TO_COMPRESSION_PROJECTION_LEDGER_V0.1.json`  
**Persistent record state:** `FROZEN`  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

R19 initially closes projection accounting:

```text
345 effective parse units
345 primary projection entries
120 compression items
0 unmapped parse units
0 duplicate primary owners
```

The transport test therefore attacks semantic loss and standing leakage rather than mere coverage.

## 1. Attack matrix

```text
PROJECTION_ASSERTION_WITHOUT_LEDGER                   = CONTAINED
UNMAPPED_PARSE_UNIT                                   = CONTAINED
DUPLICATE_PRIMARY_OWNER                               = CONTAINED
SOURCE_RECURRENCE_AS_INDEPENDENT_WARRANT              = CONTAINED
FORMAL_VARIANT_AS_CANONICAL_EQUIVALENCE               = CONTAINED
THEORY_OR_AXIOM_AS_RESULT                             = CONTAINED
BENCHMARK_SPECIFICATION_AS_EXECUTION                  = CONTAINED
EXPERIMENT_SPECIFICATION_AS_EXECUTION                 = CONTAINED
OBSERVED_PHENOMENON_LABEL_AS_EXECUTION_EVIDENCE       = CONTAINED
EXTERNAL_MAPPING_AS_ENDPOINT_IDENTITY                 = CONTAINED
CURRENT_HEAD_AS_COMPLETE_HISTORY                      = CONTAINED
SECONDARY_AUDIT_AS_PRIMARY_SOURCE                     = CONTAINED

UPSTREAM_MIXED_STANDING_UNITIZATION                   = HIT
MAPPED_DISTINCTION_DROPPED_BY_SINGLE_STANDING_SUMMARY = HIT_DERIVED_FROM_UPSTREAM_PARSE
```

No new compression coordinate is demonstrated. The failure localizes to effective parse unitization.

## 2. Why this is an upstream parse failure

Amendment 001 to the exhaustive parse contract requires source-coincident splitting when a single source block contains independently recognizable standings.

The following historical R19 parse units are too coarse:

### R19P0024 — README existing-systems block

Historical unit standing:

```text
APPLICATION_EXAMPLE
```

but it contains both:

```text
source assertions about current LLM representation/generation/reality coupling/failure modes
AND
applications/examples involving biological intelligence and adaptive memory substrates
```

The compression summary under `R19:README:EXAMPLES` cannot truthfully own the LLM assertion standing as merely an example.

### R19P0025 — README experimental/status block

Historical unit standing:

```text
METHODOLOGY_OR_TEST_PROTOCOL
```

but it contains both:

```text
proposed measurements / next empirical stages
AND
SOURCE STATUS: repository is a theoretical framework
```

The status needs its own source-coincident unit.

### R19P0037 — benchmark error curve

Historical unit standing:

```text
DEFINITION
```

but it contains:

```text
error-curve definition
AND
prediction/claim that the curve rises
```

### R19P0039 — benchmark false demotion

Historical unit standing:

```text
DEFINITION
```

but it contains:

```text
false-demotion definition
AND
good-result criterion that memory gravity and false demotion both be low
```

### R19P0058 — human counterexample

Historical unit standing:

```text
APPLICATION_EXAMPLE
```

but it contains both the distributed-loop example and a conceptual assertion that human intelligence is stabilized by an external correction network / civilization-scale feedback.

### R19P0073 — correction frequency

Historical unit standing:

```text
DEFINITION
```

but it also asserts that high correction frequency is not automatically better.

### R19P0187 — Azus Mesh/permeability mapping

Historical unit standing:

```text
APPLICATION_EXAMPLE
```

but it contains a distinct proposed measurement formula:

```text
Theta = externally forced structural updates / total structural updates
```

### R19P0193 — Azus telemetry/final mapping

Historical unit standing:

```text
FORMAL_STRUCTURE
```

but it combines the telemetry vector with a conceptual/testbed claim that Azus is approximately a visible laboratory for adaptive dynamics and frames the ability to become less wrong as the target property.

### R19P0194 — simulation title/objective

Historical unit standing:

```text
IDENTITY_TITLE
```

but it also contains experiment scope/objective: test stability conditions of a correction loop rather than intelligence directly.

### R19P0218 — simulation falsification/core claim

Historical unit standing:

```text
METHODOLOGY_OR_TEST_PROTOCOL
```

but it combines falsification criteria with the substantive hypothesis that adaptive structure cannot sustainably outpace reality-mediated correction.

### R19P0232 — frequency/magnitude examples

Historical unit standing:

```text
APPLICATION_EXAMPLE
```

but it also asserts that no single frequency/magnitude balance is universally optimal.

### R19P0279 — correction efficiency

Historical unit standing:

```text
DEFINITION
```

but it also asserts that healthy systems repair efficiently while failing systems require increasing effort for decreasing correction.

These are source-block mixed-standing collapses. The compression did not create them; it exposed them by forcing each destination to carry a single standing.

## 3. Minimal sufficient revision

Required parse repair:

- preserve each historical unit and its provenance;
- overlay an effective-content narrowing for the original standing;
- add one source-coincident unit for the second standing;
- change no source locator;
- change no global parser role;
- add no compression coordinate.

Twelve source blocks require splitting, so the effective parse grows from:

```text
287 parsed + 58 unresolved = 345
```

to:

```text
299 parsed + 58 unresolved = 357
```

The new units are to occupy:

```text
R19P0288-R19P0299
```

No unresolved unit count changes.

## 4. Compression consequences

The current 120-item compression is historical and remains frozen.

A local successor overlay must:

- narrow content/source-unit ownership of affected existing items;
- add a README status item;
- add a simulation scope item;
- route the ten other new split units into already-existing standing-compatible destinations;
- preserve all unaffected items.

Expected effective compressed-item count after repair:

```text
122
```

Projection must be reclosed at 357 units.

## 5. What did *not* fail

The death test did not require a new representation for:

```text
formal variants
same-symbol conflicts
source recurrence
external endpoint ambiguity
execution standing
bounded absence
historical provenance
```

Existing mechanisms already preserve those pressures.

Therefore:

```text
NEW_EPISTEMIC_DISTINCTION_REQUIRED   = NO
NEW_GLOBAL_PARSER_ROLE                = NONE
NEW_TOP_LEVEL_COMPRESSION_COORDINATE  = NONE
BASE_PARSE_CONTRACT_AMENDMENT         = NOT_EARNED
AMENDMENT_005                         = NOT_EARNED
```

## 6. Verdict

```text
R19_COMPRESSION_TRANSPORT_V0.1         = FAILED_PENDING_LOCAL_UPSTREAM_REPAIR
FAILURE_LOCUS                          = PARSE / MIXED-STANDING UNITIZATION
POST_COMPRESSION_ONTOLOGY_REPAIR       = NONE
R19_REUSABLE_NODE_STATE                = NOT_YET_EARNED
Z19_EFFECTIVE_NODE_STATE               = NOT_YET_EARNED
R20_PROGRAM_PARSE_ACCESS               = NOT_YET_OPENED
PROPAGATE_KERNEL                       = NOT_EARNED
```

The compression death test therefore performs its intended role: it does not paper over an upstream unitization error by inventing a richer compression object.
