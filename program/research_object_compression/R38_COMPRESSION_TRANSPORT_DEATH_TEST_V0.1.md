# R38 COMPRESSION TRANSPORT — DEATH TEST V0.1

**Repository:** `bjoern-janson/magikarp`  
**Frozen head:** `876dbe9fae2d2ddb93419b1072162dc1491d3d7f`  
**Frozen root tree:** `9d89c9d91bd187a1cb448ba93422f3fbe4addf36`  
**Effective source surface:** 47 blobs = 44 admitted + 3 explicit exclusions  
**Effective exhaustive parse:** 130 units / 0 unresolved  
**Effective compression:** 130 identity-sized standing/provenance-pure items  
**Effective projection:** 130 exact direct primary owners  
**Persistent record state:** `FROZEN`  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

## 1. Terminal accounting

```text
R38_TOTAL_BLOB_PATHS                    = 47
R38_EFFECTIVE_SOURCE_PATHS              = 44
R38_SCOPE_EXCLUDED_PATHS                = 3
R38_UNENUMERATED_PATHS                  = 0

R38_EFFECTIVE_PARSE_UNITS               = 130
R38_PARSED_REPRESENTATIONS              = 130
R38_EXPLICITLY_UNRESOLVED               = 0
R38_PARSER_FAILURES                     = 0
R38_EXTERNAL_PROVENANCE_UNITS           = 4

R38_PRIMARY_PROJECTION_ENTRIES          = 130
R38_DIRECT_PROJECTION_ENTRIES           = 130
R38_PRIMARY_COMPRESSED_ITEMS            = 130
R38_UNMAPPED_PARSE_UNITS                = 0
R38_DUPLICATE_PRIMARY_OWNERS            = 0
R38_EXTRA_PROJECTION_UNITS              = 0
R38_SOURCE_STANDING_MISMATCHES          = 0
```

Canonical projection-pair list SHA-256:

`81ba70acfcc1e6d5affbe5544bf30ed9c2f8b38df026e75532acdb43b5fc69e5`

## 2. Why compression remains identity-sized

R38's scientific value depends on keeping apart states that are superficially adjacent:

```text
engineering execution
!= evidence eligibility
!= evidence-bearing result

observed engineering delta
!= valid scientific null

no evidence-bearing run
!= valid negative

L0 built-in source
!= hypothetical L2 fixture
!= actual L2/L3 source

snapshot base
!= tested freeze package
!= post-freeze archival head
```

Further grouping would merge at least one standing, evidence-state, source/external provenance, or Git-state distinction.

```text
LOSS_BOUNDED_COMPRESSION != REQUIRED_ITEM_COUNT_REDUCTION
STANDING_AND_PROVENANCE_PURITY > ARTIFICIAL_COMPRESSION_RATIO
```

## 3. Frozen scientific question firewall

The v0.1 primary comparison remains:

```text
M0: R_c ~ Q + A + E
M1: R_c ~ Q + A + E + SD
```

The frozen claim type is predictive.

```text
PREDICTIVE_INCREMENT != CAUSAL_EFFICACY
M1_BETTER_THAN_M0 != do(SD_UP)->Y_ADAPT_UP
```

No causal authority is emitted.

## 4. Diagnosis / revision / outcome firewall

Transport preserves:

```text
F -> q_SD -> D_revision -> Y_adapt
```

and:

```text
DIAGNOSTIC_FAILURE
!= REVISION_GOVERNANCE_FAILURE
!= EXECUTION_ADAPTATION_FAILURE
```

Revision choice and recovery cannot retroactively define the diagnosis.

## 5. Interface boundary firewall

The source supplies three revision-depth classes and constructs interface failure so depth 2 is the minimum sufficient supplied repair.

```text
INTERFACE_DIAGNOSIS != INTERFACE_INVENTION
DEPTH_2_SELECTION != REPRESENTATION_INVENTION
SUPPLIED_CORRECTION_ARCHITECTURE != AUTONOMOUS_OPERATOR_DISCOVERY
```

## 6. Current evidence-state firewall

The frozen project state is:

```text
QUALIFYING_L2_SOURCES       = 0
QUALIFYING_L3_SOURCES       = 0
EVIDENCE_BEARING_RUNS       = 0
EMPIRICAL_POSITIVE_CLAIM    = FALSE
EMPIRICAL_VALID_NEGATIVE    = FALSE
```

Therefore:

```text
NO_EVIDENCE_RUN != NEGATIVE_RESULT
```

R38 is an instrument/governance/execution neuron, not a positive or negative scientific-result neuron.

## 7. Independently verified CI execution

R38 ingestion independently retrieved GitHub Actions run:

```text
RUN_ID = 31252073027
CONCLUSION = success
```

Jobs:

```text
93089832343 = test-and-smoke (Python 3.11.15)
93089832323 = test-and-smoke (Python 3.12.13)
```

Each retrieved job log shows:

```text
UNIT_TESTS = 61
UNIT_TEST_RESULT = OK
DETERMINISTIC_ENGINEERING_SMOKE = EXECUTED
SMOKE_BOUNDARY_ASSERTION = PASSED
```

This is genuine execution provenance.

```text
REPOSITORY_CI_EXECUTION != INDEPENDENT_REPLICATION
```

## 8. Observed smoke-output firewall

Both retrieved runtime logs report:

```text
delta_MAE = -0.005430183597072646
95% interval = [-0.006750692500288286, -0.0029628877700948186]
validity_passed = true
independence_level = L0
status = engineering_only
evidence_ready = false
evidence_bearing = false
```

Transport preserves the decisive ceiling:

```text
NEGATIVE_ENGINEERING_DELTA != VALID_SCIENTIFIC_NEGATIVE
GREEN_VALIDITY_GATES != EVIDENCE_ELIGIBILITY
```

The number may be retained as an observed engineering diagnostic only.

## 9. Valid-null firewall

The source deliberately retains valid nulls when a qualifying evidence run exists.

```text
valid_negative = eligible + frozen + attested + validity_passed + positive_rule_not_met
```

By contrast:

```text
benchmark_invalid != valid_negative
engineering_only != valid_negative
```

A failed validity/provenance/eligibility condition licenses no empirical negative claim.

## 10. Built-in independence firewall

The built-in source is irreducibly:

```text
structural_family_id = builtin_latent_context_v0.1
independence_level = L0
```

Its `generator_family` labels are buckets over that one mechanism.

```text
BUCKET_VARIATION != STRUCTURAL_FAMILY_VARIATION
REPRODUCIBILITY != STRUCTURAL_INDEPENDENCE
MORE_SEEDS != HIGHER_INDEPENDENCE_LEVEL
```

No metadata, wrapper, attestation, or resealing promotes it.

## 11. Independence-tooling firewall

R38 includes mature machinery for provenance, manifests, eligibility, attestations, frozen designs, and independent-source handoff.

```text
INDEPENDENCE_TOOLING != INDEPENDENT_SOURCE
HYPOTHETICAL_L2_TEST_FIXTURE != REAL_L2_GENERATOR
INDEPENDENT_GENERATOR_BRIEF != EXECUTED_INDEPENDENT_GENERATOR
```

The machinery earns engineering/governance standing only until a real source satisfies the external conditions.

## 12. Attestation-authority firewall

Software can verify schema and exact bindings but cannot establish all social facts by itself.

```text
HASH_BINDING != SOCIAL_INDEPENDENCE
SCHEMA_VALIDITY != CONTROL_INDEPENDENCE
ATTESTOR_FIELD != EXTERNALLY_PROVEN_INDEPENDENT_ACTOR
```

The source itself declares this ceiling.

## 13. Generalization / independence firewall

The analysis performs matched group/bucket holdout and paired bootstrap while carrying `structural_family_id` separately.

```text
GENERATOR_BUCKET_HOLDOUT != STRUCTURAL_FAMILY_HOLDOUT
BUCKET_BOOTSTRAP != EXTERNAL_REPLICATION
SOURCE_OCCURRENCE_MULTIPLICITY != WARRANT_INDEPENDENCE
```

No pseudoreplication defense is inflated into external generalization authority.

## 14. Test-source / test-execution firewall

All five test files attack the frozen invariants, including authority laundering, label leakage, config drift, manifest divergence, validity failure, pseudoreplication, and evidence-run bypass.

```text
TEST_SOURCE != TEST_EXECUTION
```

The retrieved CI logs are the execution witness; source files alone are not.

## 15. Tested Git state / merged freeze / release-head firewall

The project-state snapshot base is:

`57d3c526ee930a525f0dff39c162aa2c68863abb`.

The PR head is:

`dfdf0efdd2d51acb88720bd084f71a9e753572f5`.

The tested PR merge is:

`e78e310410b562105273e8e6d644504ea4e6cc4b`.

The actual merged freeze commit is:

`a14729326790678d4410cd0b442bf5540c3ea409`.

The latter three states resolve to the same tree:

`d599f9d6ce047df4afea2f519c22732589e0e06c`.

The frozen R38 head is:

`876dbe9fae2d2ddb93419b1072162dc1491d3d7f`

with tree:

`9d89c9d91bd187a1cb448ba93422f3fbe4addf36`.

Direct comparison from the tested PR head to the frozen R38 head shows only `.zenodo.json` added after the tested freeze tree.

Therefore:

```text
SNAPSHOT_BASE != TESTED_FREEZE_PACKAGE
TESTED_PR_TREE = TESTED_MERGE_TREE = MERGED_FREEZE_TREE
MERGED_FREEZE_TREE != ARCHIVAL_HEAD_TREE_ONLY_BY_ZENODO_METADATA
```

No claim is made that the freeze package was already present unchanged at the snapshot base.

## 16. Freeze-document claim / Git verification firewall

PR #1 states that the freeze package changes no scientific benchmark meaning. Cerebro does not accept that prose alone as proof.

The stronger bounded observation is:

- the retrieved CI ran the PR merge tree;
- PR head, CI merge, and merged freeze commit share the same tree;
- current scientific/implementation/test/workflow blobs match that tested freeze package;
- only `.zenodo.json` appears afterward at the R38 head.

```text
SOURCE_CLAIM_OF_NO_SCIENTIFIC_CHANGE != INDEPENDENT_CAUSAL_PROOF
DIRECT_TREE_IDENTITY_AND_FILE_COMPARE = BOUNDED_GIT_STATE_EVIDENCE
```

## 17. Broader-concept firewall

CARP, Representational Governance, CRE, Correctable Lineage context, Negative-Space Search context, escape topology, authority capture, Gyarados failure, and related reserve concepts remain source-local conceptual surfaces.

```text
CONCEPTUAL_FRAMEWORK != VALIDATED_MAGIKARP_RESULT
MAGIKARP_RESULT != CRE_VALIDATION
LINEAGE_NARRATIVE != EMPIRICAL_WARRANT
```

## 18. Relation/map firewall

The repository contains conceptual lineage assertions.

```text
SOURCE_RELATION_ASSERTION
!= ENDPOINT_IDENTITY
!= RESOLVED_EDGE
!= COMPOSITION
!= AUTHORITY
```

No R38 map edge is emitted.

## 19. Warrant-multiplicity firewall

The same authority rule is repeated across documentation, implementation, tests, workflow source, CI logs, and archive metadata.

```text
SOURCE_OCCURRENCE_COUNT != WARRANT_INDEPENDENCE != WARRANT_MULTIPLICITY
```

Default independence remains `NOT_ESTABLISHED`; multiplicity effect remains `NONE` unless separately earned.

## 20. Commit chronology

Initial repository commit:

`b4108899d18cb183c647cb9069fe631d6e056cf8`.

Git comparison places the frozen head 45 commits ahead, yielding 46 reachable commits including the initial object in that comparison.

```text
COMMIT_ORDER != SEMANTIC_EDGE
DEVELOPMENTAL_CHRONOLOGY != SCIENTIFIC_REPLICATION
```

## 21. Compression integrity

The explicit projection ledger records:

```text
130 parse units
-> 130 DIRECT_TO_COMPRESSION_ITEM entries
-> 130 primary compressed items
```

with:

```text
UNMAPPED_PARSE_UNITS       = 0
DUPLICATE_PRIMARY_OWNERS   = 0
EXTRA_PROJECTION_UNITS     = 0
STANDING_MISMATCHES        = 0
```

The four external GitHub provenance units remain external provenance items after compression and do not become repository source paths.

## 22. Transport attack matrix

```text
ENGINEERING_EXECUTION_AS_SCIENTIFIC_EVIDENCE            = CONTAINED
NEGATIVE_SMOKE_DELTA_AS_VALID_SCIENTIFIC_NULL            = CONTAINED
ZERO_EVIDENCE_RUNS_AS_NEGATIVE_RESULT                    = CONTAINED
GREEN_VALIDITY_AS_EVIDENCE_ELIGIBILITY                   = CONTAINED
L0_BUCKETS_AS_STRUCTURAL_REPLICATION                     = CONTAINED
REPRODUCIBILITY_AS_INDEPENDENCE                          = CONTAINED
INDEPENDENCE_TOOLING_AS_INDEPENDENT_SOURCE               = CONTAINED
HYPOTHETICAL_L2_FIXTURE_AS_ACTUAL_L2_SOURCE              = CONTAINED
ATTESTATION_SCHEMA_AS_SOCIAL_INDEPENDENCE                = CONTAINED
BUCKET_BOOTSTRAP_AS_EXTERNAL_GENERALIZATION              = CONTAINED
DIAGNOSIS_AS_REVISION_CHOICE                             = CONTAINED
REVISION_CHOICE_AS_RECOVERY                              = CONTAINED
INTERFACE_DIAGNOSIS_AS_INTERFACE_INVENTION               = CONTAINED
SUPPLIED_DEPTH2_AS_AUTONOMOUS_REPRESENTATION_INVENTION   = CONTAINED
PREDICTIVE_GAIN_AS_CAUSAL_EFFICACY                       = CONTAINED
CARP_OR_RG_OR_CRE_AS_V0_1_EMPIRICAL_RESULT               = CONTAINED
SOURCE_LINEAGE_AS_CEREBRO_EDGE                           = CONTAINED
SNAPSHOT_BASE_AS_TESTED_FREEZE_PACKAGE                   = CONTAINED
TESTED_FREEZE_TREE_AS_ARCHIVAL_HEAD_TREE_IDENTITY        = CONTAINED
CI_EXECUTION_AS_INDEPENDENT_REPLICATION                  = CONTAINED
TEST_SOURCE_AS_TEST_EXECUTION                             = CONTAINED
SOURCE_OCCURRENCE_AS_INDEPENDENT_WARRANT                 = CONTAINED
```

No transport failure remains.

## 23. Repair locality

```text
R38_SOURCE_SURFACE_REPAIR       = NONE
R38_PARSE_REPAIR                = NONE
R38_COMPRESSION_REPAIR          = NONE
R38_PROJECTION_REPAIR           = NONE
R38_POST_TRANSPORT_REPAIR       = NONE
NEW_EPISTEMIC_DISTINCTION       = NO
NEW_GLOBAL_PARSER_ROLE          = NONE
NEW_COMPRESSION_COORDINATE      = NONE
AMENDMENT_005                   = NOT_EARNED
```

## 24. Transportability result

```text
EFFECTIVE_COMPRESSION_CONTRACT_TRANSPORT = SUPPORTED_ON_R01_R38_FROZEN_HEADS
```

This is a bounded historical transport statement only.

## 25. Final R38 verdict

```text
R38_SOURCE_SURFACE                    = FROZEN_COMPLETE_47_BLOB_HEAD
R38_FROZEN_HEAD_COMMIT                = 876dbe9fae2d2ddb93419b1072162dc1491d3d7f
R38_FROZEN_ROOT_TREE                  = 9d89c9d91bd187a1cb448ba93422f3fbe4addf36
R38_TOTAL_BLOB_PATHS                  = 47
R38_RESEARCH_BEARING_PATHS            = 44
R38_SCOPE_EXCLUDED_PATHS              = 3
R38_UNENUMERATED_PATHS                = 0

R38_EFFECTIVE_EXHAUSTIVE_PARSE_UNITS  = 130
R38_PARSED_REPRESENTATIONS            = 130
R38_UNRESOLVED_SOURCE_UNITS           = 0
R38_PARSER_FAILURES                   = 0
R38_EXTERNAL_PROVENANCE_UNITS         = 4

R38_PRIMARY_PROJECTION_ENTRIES        = 130
R38_DIRECT_PROJECTION_ENTRIES         = 130
R38_PRIMARY_COMPRESSED_ITEMS          = 130
R38_UNMAPPED_PARSE_UNITS              = 0
R38_DUPLICATE_PRIMARY_OWNERS          = 0
R38_SOURCE_STANDING_MISMATCHES        = 0

R38_VERIFIED_CI_RUN                   = 31252073027
R38_VERIFIED_CI_JOBS                  = 2
R38_VERIFIED_TESTS_PER_JOB            = 61
R38_VERIFIED_ENGINEERING_STATUS       = engineering_only
R38_VERIFIED_ENGINEERING_DELTA_MAE    = -0.005430183597072646
R38_VERIFIED_ENGINEERING_CI95         = [-0.006750692500288286,-0.0029628877700948186]
R38_BUILTIN_INDEPENDENCE_LEVEL        = L0
R38_QUALIFYING_L2_SOURCE_COUNT        = 0
R38_QUALIFYING_L3_SOURCE_COUNT        = 0
R38_EVIDENCE_BEARING_RUN_COUNT        = 0
R38_EMPIRICAL_POSITIVE_CLAIM          = NONE
R38_EMPIRICAL_VALID_NEGATIVE_CLAIM    = NONE

R38_SOURCE_SURFACE_REPAIR             = NONE
R38_PARSE_REPAIR                      = NONE
R38_COMPRESSION_REPAIR                = NONE
R38_PROJECTION_REPAIR                 = NONE
R38_POST_TRANSPORT_REPAIR             = NONE

R38_REUSABLE_NODE_STATE               = EARNED
Z38_EFFECTIVE_NODE_STATE              = EARNED
R38_MAP_EDGE_EMISSION                 = NONE
R38_MAP_AUTHORITY                     = NONE
R38_SCIENTIFIC_AUTHORITY              = NONE
PROPAGATE_KERNEL                      = NOT_EARNED
AMENDMENT_005                         = NOT_EARNED

Z01_Z38_REUSABLE_NODE_STATE           = EARNED
R39_PROGRAM_PARSE_ACCESS              = NEXT_AUTHORIZED_REPOSITORY
```

`Z38_EFFECTIVE_NODE_STATE` is earned by the frozen complete source surface, exhaustive standing-separated parse, verified CI/execution provenance with preserved non-scientific authority ceiling, exact one-owner compression transport, and zero unresolved distinctions after death testing.

No scientific positive or negative is earned by R38 itself.
