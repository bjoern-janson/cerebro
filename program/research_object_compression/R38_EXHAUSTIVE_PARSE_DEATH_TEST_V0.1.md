# R38 EXHAUSTIVE PARSE — DEATH TEST V0.1

**Repository:** `bjoern-janson/magikarp`  
**Frozen head:** `876dbe9fae2d2ddb93419b1072162dc1491d3d7f`  
**Frozen root tree:** `9d89c9d91bd187a1cb448ba93422f3fbe4addf36`  
**Effective source surface:** 47 blobs = 44 admitted + 3 explicit exclusions  
**Candidate parse:** 130 standing-pure units = 126 source-derived + 4 external provenance units  
**State:** `FROZEN`

## 1. Coverage attack

```text
TOTAL_BLOBS                 = 47
ADMITTED_SOURCE_PATHS       = 44
EXPLICIT_EXCLUSIONS         = 3
SOURCE_PATHS_COVERED        = 44
UNENUMERATED_PATHS          = 0
PARSE_UNITS                 = 130
PARSER_FAILURES             = 0
CANDIDATE_UNRESOLVED        = 0
```

The four GitHub execution/Git-state units do not change the source aperture; they are typed as external ingestion provenance.

**Verdict:** `CONTAINED`.

## 2. Engineering execution / scientific evidence attack

The repository deliberately permits deterministic built-in smoke execution while prohibiting scientific authority for the built-in source.

```text
ENGINEERING_EXECUTION != EVIDENCE_ELIGIBILITY != EVIDENCE_BEARING_RESULT
```

The independently retrieved CI execution itself reports:

```text
independence_level = L0
status             = engineering_only
evidence_ready     = false
evidence_bearing   = false
validity_passed    = true
```

Passing engineering validity therefore does not silently become a scientific result.

**Verdict:** `CONTAINED`.

## 3. Observed negative delta / valid scientific null attack

The retrieved smoke output is:

```text
delta_MAE = -0.005430183597072646
95% interval = [-0.006750692500288286, -0.0029628877700948186]
```

But the source is L0 and explicitly ineligible. Therefore:

```text
OBSERVED_NEGATIVE_ENGINEERING_DELTA != VALID_SCIENTIFIC_NEGATIVE
```

and:

```text
NO_EVIDENCE_BEARING_RUN != VALID_NEGATIVE
```

The parse records the number as engineering execution output only. It does not interpret it as evidence against H1.

**Verdict:** `CONTAINED`.

## 4. Valid negative / benchmark invalid attack

The source and implementation distinguish:

```text
valid_negative != benchmark_invalid
```

A valid negative requires evidence mode, eligible independent source, all validity gates, frozen/attested execution, and failure of the preregistered positive rule. Failed validity/provenance/eligibility authorizes no scientific null.

**Verdict:** `CONTAINED`.

## 5. L0 / L2-L3 independence attack

The built-in source is one structural mechanism with seed/index buckets.

```text
GENERATOR_BUCKETS != STRUCTURAL_FAMILIES
REPRODUCIBILITY != STRUCTURAL_INDEPENDENCE
L0 != L2 != L3
```

No number of resealed metadata objects, wrappers, seeds, or attestations may promote the built-in source.

**Verdict:** `CONTAINED`.

## 6. Independence machinery / independent source attack

R38 implements manifest, provenance, attestation, eligibility, and restricted-generator handoff machinery. It also contains a hypothetical L2 schema fixture in tests.

```text
INDEPENDENCE_SCHEMA != INDEPENDENT_SOURCE
HYPOTHETICAL_L2_FIXTURE != ACTUAL_L2_GENERATOR
INDEPENDENT_GENERATOR_BRIEF != INDEPENDENT_GENERATOR
```

Current source-reported state remains L2 count 0 and L3 count 0.

**Verdict:** `CONTAINED`.

## 7. Software attestation / social independence attack

The validator can check schema, exact hashes, declared independence level, and consistency. It explicitly cannot establish social independence, control independence, or immutable external custody merely by computation.

```text
ATTESTATION_SCHEMA_VALIDATION != SOCIAL_INDEPENDENCE
HASH_BINDING != CONTROL_INDEPENDENCE
RECORD_LOCATOR_STRING != EXTERNALLY_PROVEN_IMMUTABILITY
```

**Verdict:** `CONTAINED`.

## 8. Bootstrap unit / warrant independence attack

The built-in analysis can bootstrap or hold out generator buckets. All built-in buckets share one structural family.

```text
BUCKET_BOOTSTRAP != STRUCTURAL_REPLICATION
GENERATOR_OCCURRENCE_MULTIPLICITY != WARRANT_INDEPENDENCE
```

The analysis retains structural-family identity precisely so bucket multiplicity cannot become scientific warrant multiplicity.

**Verdict:** `CONTAINED`.

## 9. Diagnosis / revision / outcome attack

The load-bearing chain is preserved:

```text
F -> q_SD -> D_revision -> Y_adapt
```

with separate failure loci:

```text
DIAGNOSTIC_FAILURE
!= REVISION_GOVERNANCE_FAILURE
!= EXECUTION_ADAPTATION_FAILURE
```

Neither revision choice nor recovery is allowed to retroactively define diagnosis.

**Verdict:** `CONTAINED`.

## 10. Interface diagnosis / invention attack

The synthetic benchmark establishes a supplied depth-2 interface-expansion class as the sufficient action for controlled interface failures.

```text
INTERFACE_INADEQUACY_DIAGNOSIS != INTERFACE_INVENTION
SUPPLIED_DEPTH_2_EXPANSION != AUTONOMOUS_REPRESENTATION_INVENTION
SELECTION_AMONG_SUPPLIED_REVISIONS != OPERATOR_INVENTION
```

**Verdict:** `CONTAINED`.

## 11. Prediction / causality attack

The frozen primary comparison is:

```text
M0: R_c ~ Q + A + E
M1: R_c ~ Q + A + E + SD
```

Even a future valid positive would support bounded held-out predictive increment, not:

```text
do(SD up) -> Y_adapt up
```

or broad causal efficacy.

**Verdict:** `CONTAINED`.

## 12. Broader conceptual surfaces attack

CARP, Representational Governance, CRE, escape topology, authority capture, transformation topology, Gyarados failure, and related objects are candidate frameworks/reserve/future research context.

```text
CONCEPTUAL_CONTEXT != V0_1_EMPIRICAL_RESULT
FUTURE_RESEARCH_OBJECT != CURRENT_LOAD_BEARING_ONTOLOGY
```

A MAGIKARP v0.1 result cannot automatically grant authority to those objects.

**Verdict:** `CONTAINED`.

## 13. Source lineage / Cerebro edge attack

R38 narrates conceptual lineage to Correctable Lineage and Negative-Space Search.

```text
SOURCE_LINEAGE_ASSERTION
!= ENDPOINT_IDENTITY
!= RESOLVED_RELATION
!= CEREBRO_EDGE
!= COMPOSITION
!= AUTHORITY
```

No R38 edge is emitted.

**Verdict:** `CONTAINED`.

## 14. Snapshot base / tested freeze / archival head attack

Three Git states must not be collapsed:

```text
PROJECT_FREEZE_SNAPSHOT_BASE = 57d3c526ee930a525f0dff39c162aa2c68863abb
PR_HEAD                      = dfdf0efdd2d51acb88720bd084f71a9e753572f5
TESTED_PR_MERGE              = e78e310410b562105273e8e6d644504ea4e6cc4b
MERGED_FREEZE_COMMIT         = a14729326790678d4410cd0b442bf5540c3ea409
FROZEN_R38_HEAD              = 876dbe9fae2d2ddb93419b1072162dc1491d3d7f
```

The PR head, tested PR merge, and merged freeze commit all resolve to the same tree:

`d599f9d6ce047df4afea2f519c22732589e0e06c`.

The R38 head resolves to:

`9d89c9d91bd187a1cb448ba93422f3fbe4addf36`.

Direct comparison shows the only later file difference is `.zenodo.json` archival metadata.

Therefore:

```text
SNAPSHOT_BASE != TESTED_FREEZE_PACKAGE
TESTED_FREEZE_TREE = MERGED_FREEZE_TREE
TESTED_FREEZE_TREE != RELEASE_HEAD_TREE_ONLY_BY_ARCHIVAL_METADATA
```

No false claim is made that all freeze-package artifacts existed unchanged at the snapshot base.

**Verdict:** `CONTAINED`.

## 15. Green CI / scientific result attack

GitHub Actions run `31252073027` is a genuine execution witness. Both matrix jobs passed 61 tests and smoke-boundary assertions.

```text
CI_EXECUTION_RECORD != INDEPENDENT_REPLICATION
GREEN_CI != SCIENTIFIC_RESULT
TEST_SOURCE != TEST_EXECUTION
```

The retrieved run strengthens engineering/execution provenance only.

**Verdict:** `CONTAINED`.

## 16. Source occurrence / warrant multiplicity attack

The same evidence boundary appears in README, freeze docs, protocol, code, tests, CI logs, and archive metadata.

```text
SOURCE_OCCURRENCE_COUNT != WARRANT_INDEPENDENCE != WARRANT_MULTIPLICITY
```

Default independence remains `NOT_ESTABLISHED`; multiplicity effect remains `NONE` unless separately identified.

**Verdict:** `CONTAINED`.

## 17. Historical chronology attack

Initial commit:

`b4108899d18cb183c647cb9069fe631d6e056cf8`

Frozen head is 45 commits ahead in Git comparison, yielding 46 reachable commits including the initial object.

```text
COMMIT_ORDER != SEMANTIC_EDGE
COMMIT_ORDER != CAUSAL_VALIDATION
DEVELOPMENTAL_REPETITION != INDEPENDENT_WARRANT
```

**Verdict:** `CONTAINED`.

## 18. Zero-unresolved audit

The candidate zero-unresolved state survives independent discrimination because:

- the negative smoke number has a lower engineering standing rather than scientific-null standing;
- zero evidence-bearing runs is explicitly distinguished from a valid negative;
- L0, L2, and L3 are not normalized;
- the independent-source tooling is not mistaken for an actual independent source;
- conceptual reserve/future context remains separate from v0.1 ontology;
- exact Git tree identity resolves tested/merged freeze state while preserving snapshot-base and archive-head differences.

```text
ZERO_UNRESOLVED != SILENT_NORMALIZATION
```

**Verdict:** `CONTAINED`.

## 19. Repair locality

```text
SOURCE_SURFACE_REPAIR       = NONE
PARSE_REPAIR                = NONE
NEW_EPISTEMIC_DISTINCTION   = NO
NEW_GLOBAL_PARSER_ROLE      = NONE
AMENDMENT_005               = NOT_EARNED
```

No contradiction requires revision of the frozen global parse contract.

## 20. Final parse verdict

```text
R38_EXHAUSTIVE_PARSE_DEATH_TEST = PASS
R38_EFFECTIVE_PARSE_UNITS       = 130
R38_PARSED_REPRESENTATIONS      = 130
R38_EXPLICITLY_UNRESOLVED       = 0
R38_PARSER_FAILURES             = 0
R38_PARSE_REPAIR                = NONE
COMPRESSION_STAGE               = OPEN
```

Map authority: `NONE`.  
Scientific authority: `NONE`.  
Propagation: `NO`.
