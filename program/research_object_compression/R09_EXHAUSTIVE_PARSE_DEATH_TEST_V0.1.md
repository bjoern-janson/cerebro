# R09 EXHAUSTIVE PARSE — DEATH TEST V0.1

**Parse candidate:** `R09_EXHAUSTIVE_PARSE_V0.1.json`  
**Source surface:** `R09_SOURCE_SURFACE_V0.1.json`  
**Persistent record state:** `FROZEN`  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

The test attacks whether the 336-unit parse preserves R09's source-relative standing and reconstructible research state without letting filenames, rhetoric, notation, malformed syntax, or source topology manufacture conclusions.

## Attack matrix

```text
LAYER_NUMBER_AS_ARTIFACT_CHRONOLOGY              = CONTAINED
INTERNAL_SELF_LABEL_AS_FILENAME_IDENTITY          = CONTAINED
UNIFIED_FINAL_BOOTLOADER_AS_SUPERSESSION          = CONTAINED
AXIOM_AS_ESTABLISHED_TRUTH                        = CONTAINED
HYPOTHESIS_AS_RESULT                              = CONTAINED
PREDICTION_AS_EVIDENCE                            = CONTAINED
NOVEL_INSIGHT_AS_RESULT                           = CONTAINED
MOCK_OR_SCHEMA_OUTPUT_AS_EVIDENCE                 = CONTAINED
PROTOCOL_AS_EXECUTION                             = CONTAINED
ARCHITECTURE_AS_IMPLEMENTATION                    = CONTAINED
FORMAL_SYMBOL_TOKEN_AS_GLOBAL_SEMANTIC_IDENTITY   = CONTAINED
OMEGA_RECONCILIATION                              = CONTAINED
MALFORMED_MARKDOWN_AS_PARSER_FAILURE              = CONTAINED
INCOMPLETE_SOURCE_AS_ABSENCE                      = CONTAINED
INTERNAL_DUPLICATION_AS_CORROBORATION             = CONTAINED
INTERNAL_DEPENDENCY_GRAPH_AS_PROGRAM_MAP_EDGE      = CONTAINED
HISTORICAL_EXAMPLE_AS_VALIDATION                   = CONTAINED
SELF_REPORTED_COMPLETENESS_AS_SCIENTIFIC_COMPLETENESS = CONTAINED
FINAL_LABEL_AS_AUTHORITY                          = CONTAINED
DEVELOPMENTAL_FORWARD_REFERENCE_OMISSION          = HIT
UNRESOLVED_INTERNAL_REFERENCE_ELISION             = HIT
```

## 1. Contained identity/order attacks

The parse preserves separately:

```text
CURRENT_FILENAME_ORDINAL
ARTIFACT_CHRONOLOGY
INTERNAL_SELF_LABEL
```

for the terminal phase-space-navigation object.

It therefore does not rewrite:

```text
layer_74_intelligence_phase_space_navigation.md
```

into an invented historical Layer 74 origin, nor does it rename the current path merely because the file internally says `Layer 73`.

```text
LAYER_NUMBER_AS_ARTIFACT_CHRONOLOGY = CONTAINED
INTERNAL_SELF_LABEL_AS_FILENAME_IDENTITY = CONTAINED
```

## 2. Contained standing attacks

The candidate maintains source-relative roles:

```text
AXIOM/ASSUMPTION
DEFINITION/FORMALIZATION
HYPOTHESIS
PREDICTION
METHOD/EXPERIMENT DESIGN
ARCHITECTURE/PROTOCOL PROPOSAL
APPLICATION/EXAMPLE
SOURCE STATUS/QUALIFIER
SOURCE FORMAT IRREGULARITY
UNRESOLVED SOURCE FRAGMENT
```

Thus:

```text
"supported" in a schema example     != reported support
mock observatory state              != measured observatory result
expected experimental result        != observed experimental result
"Novel Insight"                     != empirical result
"Final Principle"                   != scientific conclusion granted by compression
"Universal" / "Unified" / "Final" != validated universality or supersession
```

R09 contains no runnable implementation, dataset, execution record, reported empirical result, or reported negative result on the frozen head.

## 3. Contained notation attacks

The parse preserves source-local symbol semantics, including:

```text
Omega = dG_m/dt                       [Layer 5]
Omega = Delta(Delta C)/Delta t        [Layer 21]
Omega = total possibility space       [Layer 67]
Omega = phase/state space             [current file layer_74]
```

and local variation in `R` and `G_A`.

No cross-layer identity is manufactured.

```text
TOKEN_IDENTITY != SEMANTIC_IDENTITY
```

## 4. Contained malformed/incomplete-source attacks

Malformed Markdown in Layers 16, 17, 56 and 68-72 is preserved as source-format state when semantic material remains recoverable.

Layer 68's frozen terminal fragment remains:

```text
UNRESOLVED_AT_FROZEN_SOURCE_SURFACE
```

rather than becoming a parser failure, missing source object, invented completion, or negative result.

## 5. Contained recurrence attack

Layer 50 repeats its own title/purpose/sections 1-8.

The parse retains recurrence as occurrence evidence only:

```text
WARRANT_INDEPENDENCE_STATUS = NOT_ESTABLISHED
WARRANT_MULTIPLICITY_EFFECT = NONE
```

Repeated content does not become corroboration.

## 6. HIT — developmental forward-reference omission

The parse preserved most layer content but did not give explicit source-level forward research directions their own reconstructible internal-reference standing.

This matters because the source is explicitly a restoration protocol: developmental questions and named next layers are part of the research state, not decorative prose.

A concrete witness is Layer 72, which ends with the source assertion:

```text
Next logical layer:
Layer 73 — The Intelligence Compiler
```

and asks whether intelligence can be compiled, optimized and transferred between substrates.

The frozen current repository does **not** contain a current file named for `The Intelligence Compiler`.

Instead the current terminal paths include:

```text
layer_73_the_reality_coupling_threshold.md
layer_74_intelligence_phase_space_navigation.md
```

The candidate parse records Layer 72's operating-system architecture but omits this explicit next-layer referent/question.

Therefore it loses a real source distinction:

```text
SOURCE_ASSERTED_NEXT_LOGICAL_LAYER
!=
CURRENT_REPOSITORY_SUCCESSOR_ARTIFACT
```

```text
DEVELOPMENTAL_FORWARD_REFERENCE_OMISSION = HIT
```

## 7. HIT — unresolved internal reference elision

Because the source says `Layer 73 — The Intelligence Compiler` but no matching current artifact exists at the frozen head, the reference must not be silently resolved to either current Layer 73 or current Layer 74.

The correct source-relative state is:

```text
REFERENCE_ASSERTION = PRESENT
EXACT_CURRENT_ENDPOINT = UNRESOLVED_ON_FROZEN_HEAD
```

The candidate parse currently has no explicit unresolved-reference unit for this relation/question.

```text
UNRESOLVED_INTERNAL_REFERENCE_ELISION = HIT
```

This is a parse-coverage failure, not evidence that the compressor needs a new top-level semantic class.

The existing contract already contains:

```text
INTERNAL_REFERENCES
UNRESOLVED_REFERENCES
LIMITATIONS_OR_OPEN_QUESTIONS
```

so the shallowest repair is a local successor overlay.

## 8. Other developmental-reference pressure

Other sources also contain source-described next directions rather than current-artifact guarantees. Examples include source-level potential direction sets after Layers 50, 55 and 60.

These must be preserved as research directions/open questions and must not be interpreted as evidence that the named later artifacts exist, were executed, or supersede earlier work.

## 9. Death-test verdict

```text
R09_PARSE_UNITS_BEFORE_REPAIR             = 336
R09_PARSED_UNITS_BEFORE_REPAIR            = 335
R09_UNRESOLVED_UNITS_BEFORE_REPAIR        = 1
R09_PARSER_FAILURES                        = 0
DEVELOPMENTAL_FORWARD_REFERENCE_OMISSION   = HIT
UNRESOLVED_INTERNAL_REFERENCE_ELISION      = HIT
NEW_GLOBAL_PARSER_ROLE_REQUIRED            = NO
NEW_TOP_LEVEL_COMPRESSION_COORDINATE       = NOT_EARNED
AMENDMENT_005                              = NOT_EARNED
MAP_EDGE                                   = NONE
PROPAGATION                                = NONE
CEREBRO_STEP_2                             = CLOSED
```

The parse is not yet adequate for compression.

The repair target is local and exact:

1. preserve explicit source-described developmental directions as internal-reference/open-question parse units;
2. preserve Layer 72's `Intelligence Compiler` reference as unresolved against the current frozen head;
3. do not use current filename topology to fill the missing endpoint;
4. do not convert forward references into Program Map edges or roadmap authority.
