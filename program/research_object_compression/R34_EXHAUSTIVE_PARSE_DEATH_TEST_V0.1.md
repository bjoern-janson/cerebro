# R34 EXHAUSTIVE PARSE — DEATH TEST V0.1

**Repository:** `bjoern-janson/the-correctable-lineage`  
**Frozen head:** `422411ea22fb3154a5bed2b0c950ab1b3f2bba5b`  
**Frozen root tree:** `06150127b67484a2cc96f3be5540aeb1abe6ea43`  
**Source surface:** 86 blobs = 85 admitted + 1 frozen-scope exclusion  
**Parse candidate:** 126 units = 85 path-primary + 41 standing splits  
**State:** `FROZEN`

## 1. Coverage attack

```text
TOTAL_BLOB_PATHS                     = 86
PARSE_INCLUDED_PATHS                 = 85
PARSE_EXCLUDED_BY_FROZEN_SCOPE       = 1
UNENUMERATED_PATHS                   = 0
PATH_PRIMARY_PARSE_UNITS             = 85
ADMITTED_PATHS_WITHOUT_PRIMARY_UNIT  = 0
STANDING_SPLIT_PARSE_UNITS           = 41
CROSS_SOURCE_UNRESOLVED_UNITS        = 0
EFFECTIVE_PARSE_UNITS                = 126
PARSER_FAILURES                      = 0
```

The sole scope exclusion is the pure software-license text `LICENSE`. Citation, notice and documentation-license metadata remain represented as provenance metadata.

**Verdict:** `PASS`.

## 2. Current/historical precedence attack

Attack: allow broad historical corpus/formal documents to overwrite the narrower current front door.

Contained by explicit source routing and semantic standing splits:

```text
CURRENT_FRONT_DOOR != HISTORICAL_FORMULATION
HISTORICAL_VISIBILITY != CURRENT_CANONICAL_AUTHORITY
LATER_NARROWING != RETROACTIVE_DELETION
```

Older `W` semantics in `authority-acquisition-gate.md`, `authority-allocation-invariant.md`, and `evidential-update-governance.md` are preserved as historical developmental objects and explicitly marked superseded where they conflict with the current epistemic-authority / decision-authority separation.

**Verdict:** `PASS`.

## 3. Benchmark-version collapse attack

Attack: compress v0.1-v0.5 into one cumulative success narrative.

Contained:

```text
v0.1 != v0.2 != v0.3 != v0.4 != v0.5
LATER_BENCHMARK != RETROACTIVE_REPAIR_OF_EARLIER_NEGATIVE_RESULT
```

Preserved negative boundaries include:

- v0.1: no experienced contradiction before deployment -> no interface challenge under the supplied mechanism;
- v0.2: challenge availability does not imply independent/informative challenge;
- v0.3: shared failure independence is not identifiable from success-only calibration;
- v0.4: diagnosed common-mode compromise does not imply an available independent repair path;
- v0.5: evaluator-aligned scoring and supplied loss create a construct/comparative-validity ceiling.

**Verdict:** `PASS`.

## 4. Synthetic-validation attack

Attack: turn supplied targets, sensors, challenges, channels, dependency groups, policies, scopes or loss weights into system-discovered structure.

Contained:

```text
SUPPLIED_OBJECT != AUTONOMOUS_DISCOVERY
SYNTHETIC_GROUND_TRUTH != EXTERNALLY_IDENTIFIED_CAUSAL_STRUCTURE
EVALUATOR_AUTHORED_LOSS != VALIDATED_NATURAL_OBJECTIVE
BENCHMARK_SUCCESS != COMPARATIVE_OPERATIONAL_ADVANTAGE
```

The cumulative benchmark warrant remains:

```text
SUPPLIED_OBJECT_REPRESENTATIONAL_SUFFICIENCY_UNDER_SYNTHETIC_TASKS_AND_LOSSES
```

not autonomous interface invention, human usability, deployed utility, comparative superiority or general governance validation.

**Verdict:** `PASS`.

## 5. Execution-lineage attack

Attack: inflate executable source into observed execution or replication.

Contained:

```text
IMPLEMENTATION
!= TEST_SOURCE
!= WORKFLOW_SOURCE
!= EXECUTION_RECORD
!= PERSISTED_RESULT
!= INDEPENDENT_REPLICATION
```

At the frozen head, queried GitHub surfaces returned:

```text
PR_TRIGGERED_WORKFLOW_RUNS_VISIBLE_THROUGH_QUERIED_CONNECTOR = 0
COMBINED_STATUS_RECORDS_VISIBLE_THROUGH_QUERIED_CONNECTOR     = 0
```

The workflow connector is PR-trigger-filtered, so this is a bounded head-provenance statement rather than a claim that no historical workflow execution ever occurred.

Persisted benchmark result artifacts remain persisted repository results; workflow replay logic remains a reproducibility mechanism; neither is promoted to independent replication.

**Verdict:** `PASS`.

## 6. Reader-calibration execution attack

Attack: treat the persisted reader-calibration administration as blind usability evidence.

Contained:

```text
ADMINISTRATION_DRY_RUN != BLIND_RECONSTRUCTION_EVIDENCE
N_BLIND = 0
CONTAMINATED_RESPONSE != INDEPENDENT_READER_RELIABILITY
```

The dry run has genuine administration standing, but the source explicitly records prior framework exposure/contamination and refuses to count it as blind evidence.

**Verdict:** `PASS`.

## 7. Pilot-0 inflation attack

Attack: treat a complete-looking Pilot package as study execution.

Contained:

```text
PILOT_PACKAGE != PARTICIPANT_DATA
TEMPLATE != FILLED_RECORD
DRAFT_PREREGISTRATION != INDEPENDENT_REGISTRATION
CASE_SET != EMPIRICAL_RESULT
SCORING_RUBRIC != CONSTRUCT_VALIDATION
```

Participant data remain absent.

**Verdict:** `PASS`.

## 8. Comparative-protocol inflation attack

Attack: treat the mature-comparator protocol/evaluation charter as evidence that claim contracts outperform mature methods.

Contained:

```text
COMPARATIVE_PROTOCOL != COMPARATIVE_STUDY
EVALUATION_CHARTER != STUDY_EXECUTION
NATIVE_STRENGTH_REQUIREMENT != OBSERVED_COMPARATOR_RESULT
ABSORPTION_CONDITION != DISTINCTNESS_PROOF
```

**Verdict:** `PASS`.

## 9. External-review authority attack

Attack: convert a substantive external methodological correction into endorsement or validation of the downstream governance proposal.

Contained:

```text
EXTERNAL_METHODOLOGICAL_CORRECTION
!= EXTERNAL_VALIDATION_OF_CLAIM_CONTRACTS
```

The review record supports:

- absorption of Gate 1 into established identifiability theory;
- terminology correction;
- contraction of novelty authority;

while explicitly leaving downstream comparative value unresolved.

Reviewer attribution remains permission-pending and the underlying private correspondence is not reproduced in the public repository.

**Verdict:** `PASS`.

## 10. Incident-record provenance attack

Attack: turn the participation-boundaries recollection into preserved external evidence or a broad person-level judgment.

Contained:

```text
AUTHOR_RECOLLECTION != PRESERVED_INTERACTION_LOG
ONE_INTERACTION != GENERAL_CHARACTER_OR_COMPETENCE_CLAIM
```

**Verdict:** `PASS`.

## 11. Source relation -> Cerebro edge attack

Attack: use `lineage-map.md`, corpus routing or conceptual ancestry assertions to resolve repository endpoints or emit semantic edges.

Contained:

```text
SOURCE_ASSERTED_CONCEPTUAL_ANCESTRY
!= ENDPOINT_IDENTITY
!= RESOLVED_EDGE
!= COMPOSITION
!= AUTHORITY
```

No Cerebro synapse is emitted.

**Verdict:** `PASS`.

## 12. Authority-layer collapse attack

Contained current invariant:

```text
OBSERVABLE != IDENTIFIED
IDENTIFIED != EPISTEMICALLY_AUTHORIZED
EPISTEMICALLY_AUTHORIZED != DECISION_JUSTIFIED
ROBUST_ACTION != IDENTIFIED_MECHANISM
```

Historical broader authority language is retained without overriding this current split.

**Verdict:** `PASS`.

## 13. Commit chronology attack

The commit search returned its requested 100-result ceiling before reaching the repository beginning.

```text
VISIBLE_COMMIT_COUNT_LOWER_BOUND >= 100
EXACT_COMMIT_COUNT               = NOT_ESTABLISHED
```

Commit ordering remains temporal provenance only:

```text
COMMIT_ORDER != SEMANTIC_RELATION != CAUSAL_TOPOLOGY
```

**Verdict:** `PASS`.

## 14. Unresolved-status attack

No R34 source conflict equivalent to R33 Gate-002 was found.

The apparent tensions are source-resolved by explicit supersession, chronology, scope or evidence standing:

- old authority semantics vs current typed semantics;
- broad historical synthesis vs narrow current claim;
- workflow source vs absent queried head-status witness;
- reader instrument vs contaminated dry run;
- external correction vs unvalidated downstream proposal;
- Pilot/comparative design vs no execution.

Therefore:

```text
CROSS_SOURCE_UNRESOLVED_UNITS = 0
```

is supported rather than silently normalized.

**Verdict:** `PASS`.

## 15. Repair locality

```text
SOURCE_SURFACE_REPAIR = NONE
PARSE_REPAIR          = NONE
```

Two provisional inventory names were corrected from immutable tree evidence before the source surface was frozen and therefore are not post-freeze repairs.

No new global parser role or top-level compression coordinate is required.

```text
AMENDMENT_005 = NOT_EARNED
```

## 16. Final parse verdict

```text
R34_SOURCE_SURFACE_STATE       = FROZEN
R34_EXHAUSTIVE_PARSE_STATE     = FROZEN
R34_EFFECTIVE_PARSE_UNITS      = 126
R34_REPRESENTED_UNITS          = 126
R34_EXPLICIT_UNRESOLVED_UNITS  = 0
R34_PARSER_FAILURES            = 0
R34_PARSE_DEATH_TEST           = PASS
R34_PARSE_REPAIR               = NONE
R34_MAP_AUTHORITY              = NONE
R34_SCIENTIFIC_AUTHORITY       = NONE
PROPAGATE_KERNEL               = NOT_EARNED
AMENDMENT_005                  = NOT_EARNED
```

Compression is procedurally authorized on this frozen parse only.
