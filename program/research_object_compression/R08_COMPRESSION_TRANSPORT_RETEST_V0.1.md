# R08 COMPRESSION TRANSPORT — RETEST V0.1

**Death test:** `R08_COMPRESSION_TRANSPORT_DEATH_TEST_V0.1.md`  
**Compression repair:** `R08_COMPRESSION_V0.1_AMENDMENT_001.json`  
**Projection repair:** `R08_PARSE_TO_COMPRESSION_PROJECTION_LEDGER_V0.1_AMENDMENT_001.json`  
**Persistent record state:** `FROZEN`  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

The retest evaluates only the four demonstrated compression failures and preserves all previously contained R08 hostile fixtures.

## 1. `EXECUTABLE_ROLE_RECOLLAPSE`

The repaired effective node separates:

```text
SOURCE_DESCRIBED_SCOPE_OR_PURPOSE -> R08:CODE:MODULE_PURPOSES
IMPLEMENTATION_ASSUMPTION         -> R08:CODE:IMPLEMENTATION_ASSUMPTIONS
EXECUTABLE_IMPLEMENTATION         -> module-specific implementation items / R08:CODE:SIMULATION_SUITE
IMPLEMENTED_OUTPUT_BEHAVIOR       -> R08:CODE:IMPLEMENTED_OUTPUT_BEHAVIORS
SOURCE_ASSERTION_HYPOTHESIS       -> R08:HYP:SIMULATION_HYPOTHESES where present
```

Thus programmed support strings, threshold choices, synthetic parameters and implementation logic no longer inhabit one generic executable-standing object.

```text
EXECUTABLE_ROLE_RECOLLAPSE = REPAIRED
```

Non-regression:

```text
IMPLEMENTED != EXECUTED != OBSERVED != ESTABLISHED
```

No frozen execution-result artifact is admitted.

---

## 2. `HISTORICAL_CASE_ROLE_RECOLLAPSE`

Case-specific hypotheses now terminate separately:

```text
R08:HYP:HISTORICAL_AD
R08:HYP:HISTORICAL_COMPILER
R08:HYP:HISTORICAL_ML
R08:HYP:HISTORICAL_SCIENCE
```

Case proxy/test units terminate in:

```text
R08:METHOD:HISTORICAL_CASE_TESTS
```

while historical assertions and AMP interpretations remain under the case interpretation items.

```text
HISTORICAL_CASE_ROLE_RECOLLAPSE = REPAIRED
```

The repository-level qualifier remains:

```text
HISTORICAL_EXAMPLES = HYPOTHESES_FOR_TESTING_NOT_CONFIRMED_DEMONSTRATIONS
```

so strong local case rhetoric does not become empirical validation.

---

## 3. `CROSS_DOMAIN_EXAMPLE_DESTINATION_LOSS`

The affected cross-domain/example units now terminate in:

```text
R08:INTERP:CROSS_DOMAIN_EXAMPLES
```

which explicitly preserves the source's scientific-method, programming-language, compiler, automatic-differentiation, printing-press, automated-research and depth examples as interpretive examples.

```text
CROSS_DOMAIN_EXAMPLE_DESTINATION_LOSS = REPAIRED
```

No example becomes an independent warrant or map edge.

---

## 4. `ASSUMPTION_PURPOSE_RECOLLAPSE`

```text
R08:P:ASSUME:PURPOSE
```

now projects to:

```text
R08:SCOPE:PROGRAM
```

while A1–A20 and the source-labelled strongest assumption remain in:

```text
R08:ASSERT:ASSUMPTIONS
```

```text
ASSUMPTION_PURPOSE_RECOLLAPSE = REPAIRED
```

Thus:

\[
\boxed{
\text{what an assumptions document is for}
\neq
\text{what it assumes}.
}
\]

---

## 5. Effective accounting

The repair changes destinations only.

```text
R08_EFFECTIVE_PARSE_UNITS              = 396
R08_PARSED_UNITS                       = 395
R08_UNRESOLVED_PARSE_UNITS             = 1
R08_PRIMARY_PROJECTION_ENTRIES         = 395
R08_UNRESOLVED_AT_COMPRESSION_ENTRIES  = 1
R08_UNMAPPED_PARSE_UNITS               = 0
R08_DUPLICATE_PRIMARY_OWNERS           = 0
```

The unresolved unit remains:

```text
R08:P:EXP:TEMPLATE_TERMINAL
```

and is not converted into missing evidence, a parser failure or a negative result.

---

## 6. Amendment-004 recurrence non-regression

For every compressed item with multiple primary source occurrences, occurrence count remains reconstructible from the frozen ledger and the effective defaults remain:

```text
WARRANT_INDEPENDENCE_STATUS = NOT_ESTABLISHED
WARRANT_MULTIPLICITY_EFFECT = NONE
```

unless separately warranted otherwise. R08 establishes no such independence.

The exact duplicate blob at:

```text
generator_adaptation.py
recursive_improvement.py
```

remains two path occurrences, one content identity, and zero warrant multiplication.

```text
SOURCE_RECURRENCE_AS_WARRANT = CONTAINED
EXACT_DUPLICATE_BLOB_AS_WARRANT = CONTAINED
```

---

## 7. Formal-tension non-regression

The effective node still preserves separately:

```text
R08:DEF:OMEGA_DELTA_G
R08:DEF:OMEGA_DGM_DT
R08:DEF:OMEGA_OPERATIONAL_FAMILY
```

with semantic identity across them not established.

It also preserves both state representations:

```text
A_t=(S,D,X,Theta), X=(Z,M,pi,V,C,G_s,G_m,R)
```

and:

```text
S_t=(G,A,X,P,C,R), G->A->X->P->C
```

without granting the source word `canonical` automatic supersession authority.

```text
CROSS_FILE_FORMAL_RECONCILIATION = CONTAINED
CANONICAL_SELF_LABEL_AS_SUPERSESSION = CONTAINED
```

---

## 8. Empirical-standing non-regressions

```text
R08_REPORTED_EMPIRICAL_RESULTS = NONE_ON_FROZEN_SOURCE_SURFACE
R08_REPORTED_NEGATIVE_RESULTS  = NONE_ON_FROZEN_SOURCE_SURFACE
PROPOSED_PROXY_AS_VALIDATED_MEASURE = CONTAINED
PROPOSED_DATASET_AS_EMPIRICAL_DATA  = CONTAINED
SIMULATION_CODE_AS_EXECUTION_RECORD = CONTAINED
PROGRAMMED_SUPPORT_LABEL_AS_SCIENTIFIC_SUPPORT = CONTAINED
ONE_BYTE_SCRIPT_AS_NEGATIVE_RESULT = CONTAINED
```

The frozen Python files specify models, synthetic inputs, thresholds, direct-run behavior and possible interpretation branches only.

---

## 9. Transportability result

R08 required:

- recursive full-head inventory over 49 blob paths;
- exact-content/path-occurrence accounting;
- one local parse metadata-coverage repair;
- explicit preservation of one source-internal incomplete experiment fragment;
- preservation of cross-file formal tensions without reconciliation;
- a 396-unit exhaustive effective parse;
- a 396-entry projection ledger;
- local compression repairs applying already-earned role-preservation rules.

It required:

```text
NEW_GLOBAL_PARSER_ROLE = NONE
NEW_TOP_LEVEL_COMPRESSION_COORDINATE = NONE
AMENDMENT_005 = NOT_EARNED
MAP_EDGE = NONE
PROPAGATION = NONE
```

Bounded result:

```text
EFFECTIVE_COMPRESSION_CONTRACT_TRANSPORT = SUPPORTED_ON_R01_R08_FROZEN_HEADS
```

This is not universal transportability.

---

## 10. R08 verdict

```text
R08_SOURCE_SURFACE                         = FROZEN_FULL_RECURSIVE_HEAD
R08_TOTAL_BLOB_PATHS                       = 49
R08_RESEARCH_BEARING_BLOB_PATHS            = 48
R08_UNIQUE_RESEARCH_BLOBS                  = 47
R08_EFFECTIVE_EXHAUSTIVE_PARSE_UNITS       = 396
R08_PARSED_UNITS                           = 395
R08_UNRESOLVED_PARSE_UNITS                 = 1
R08_PARSE_FAILURES                         = 0
R08_PRIMARY_PROJECTION_ENTRIES             = 395
R08_UNRESOLVED_AT_COMPRESSION_ENTRIES      = 1
R08_UNMAPPED_PARSE_UNITS                   = 0
ADMITTED_CFF_FIELD_OMISSION                = REPAIRED
EXECUTABLE_ROLE_RECOLLAPSE                 = REPAIRED
HISTORICAL_CASE_ROLE_RECOLLAPSE            = REPAIRED
CROSS_DOMAIN_EXAMPLE_DESTINATION_LOSS      = REPAIRED
ASSUMPTION_PURPOSE_RECOLLAPSE              = REPAIRED
CROSS_FILE_FORMAL_RECONCILIATION           = CONTAINED
PATH_OCCURRENCE_AS_CONTENT_INDEPENDENCE    = CONTAINED
PROPOSED_PROXY_AS_VALIDATED_MEASURE        = CONTAINED
PROPOSED_DATASET_AS_EMPIRICAL_DATA         = CONTAINED
SIMULATION_CODE_AS_EXECUTION_RECORD        = CONTAINED
ONE_BYTE_SCRIPT_AS_NEGATIVE_RESULT         = CONTAINED
R08_REUSABLE_NODE_STATE                    = EARNED_WITH_ONE_EXPLICIT_UNRESOLVED_SOURCE_FRAGMENT
R08_MAP_EDGE_EMISSION                      = NONE
R08_MAP_AUTHORITY                          = NONE
R08_SCIENTIFIC_AUTHORITY                   = NONE
PROPAGATE_KERNEL                           = NOT_EARNED
CEREBRO_STEP_2                             = CLOSED
```

## 11. Sequential boundary

```text
R09_PROGRAM_PARSE_ACCESS = NEXT_AUTHORIZED_REPOSITORY
R10_R43_PROGRAM_PARSE_ACCESS = NOT_YET_OPENED
```

This authorization is procedural only and creates no R08 -> R09 semantic relation.

The first eight research neurons are reconstructible under one effective compression discipline. R08 preserves large-scale source heterogeneity, duplicate path occurrences, formal disagreement, source-internal incompleteness and executable research without manufacturing empirical standing or reconciliation.
