# R15 COMPRESSION TRANSPORT — DEATH TEST V0.1

**Compression:** `R15_COMPRESSION_V0.1.json` + Conformance 001-002  
**Effective parse:** `R15_EXHAUSTIVE_PARSE_V0.1.json` + Parts A-C  
**Projection ledger:** `R15_PARSE_TO_COMPRESSION_PROJECTION_LEDGER_V0.1.json` + Parts A-C  
**Persistent record state:** `FROZEN`  
**Map authority:** `NONE`  
**Scientific authority:** `NONE`

R15 tests whether the effective compression contract can represent a source that repeatedly reuses the same mathematical tokens for different local objects while simultaneously presenting itself as a unified/final synthesis over named prior frameworks.

The dangerous failure mode is not missing content. It is **false namespace coherence**.

## 1. Projection accounting

```text
R15_RESEARCH_SOURCE_PATHS        = 6
R15_EFFECTIVE_PRIMARY_PARSE_UNITS= 189
R15_PRIMARY_PROJECTION_ENTRIES   = 189
R15_UNMAPPED_PARSE_UNITS         = 0
R15_DUPLICATE_PRIMARY_OWNERS     = 0
R15_DERIVED_AUDIT_VIEWS          = 5
R15_COMPRESSION_SECONDARY_VIEWS  = 8
R15_DERIVED_BOUNDED_ABSENCES     = 4
```

Projection completeness passes.

## 2. Attack matrix

```text
TOKEN_IDENTITY_AS_GLOBAL_SYMBOL_IDENTITY             = CONTAINED
INTRA_PAGE_SYMBOL_OVERLOAD_AS_SINGLE_MEANING          = CONTAINED
LATEST_DEFINITION_WINS                                = CONTAINED
LOCAL_NAMESPACE_LEAKAGE_ACROSS_SECTIONS               = CONTAINED
LOCAL_SYMBOL_OVERLOAD_AS_AUTOMATIC_CONTRADICTION      = CONTAINED

ALIGNMENT_FORMS_AS_PROVED_EQUIVALENT                  = CONTAINED
OMEGA_VARIANTS_AS_ONE_CANONICAL_SET                   = CONTAINED
LOOP_VARIANTS_AS_ONE_FORMAL_OBJECT                    = CONTAINED
THEMATIC_SIMILARITY_AS_FORMAL_EQUIVALENCE             = CONTAINED

SOURCE_UNIFIED_FRAMEWORK_AS_PROGRAM_SYNTHESIS          = CONTAINED
SOURCE_FINAL_SYNTHESIS_AS_SUPERSESSION                 = CONTAINED
PAGE_NUMBER_AS_AUTHORITY_ORDER                        = CONTAINED
AIT_STACK_ORDER_AS_PROGRAM_DEPENDENCY                  = CONTAINED

SOURCE_REFERENCE_AS_RESOLVED_ENDPOINT                 = CONTAINED
SOURCE_REFERENCE_AS_PROGRAM_MAP_EDGE                  = CONTAINED
SOURCE_DESCRIPTION_OF_PRIOR_FRAMEWORK_AS_IMPORTED_TRUTH= CONTAINED
CGET_EQUILIBRIUM_LABEL_AS_R10_CANONICALIZATION         = CONTAINED

SOURCE_INVARIANT_LABEL_AS_THEOREM                     = CONTAINED
FORMAL_DEFINITION_AS_VALIDATED_METRIC                  = CONTAINED
SOURCE_HYPOTHESIS_AS_ESTABLISHED_LAW                   = CONTAINED
PROPOSED_EXPERIMENT_AS_EXECUTION                       = CONTAINED
PREDICTION_AS_REPORTED_RESULT                         = CONTAINED

RECURRENCE_AS_CORROBORATION                            = CONTAINED
SECONDARY_VIEW_AS_SECOND_WARRANT                       = CONTAINED
DERIVED_BOUNDED_ABSENCE_AS_SOURCE_ASSERTION            = CONTAINED
INTERNAL_ARROW_AS_PROGRAM_MAP_EDGE                     = CONTAINED
```

No post-projection repair is required on the current bounded attack suite.

## 3. R15's central transport result: source-local namespaces

### A / A_t

The effective node preserves at least five source-local A/A_t meanings:

```text
PAGE_00: A   = adaptation capacity
PAGE_1 loop: A_t = action
PAGE_1 alignment: A_t = alignment/correction capacity
PAGE_4 state: A_t = available action space
PAGE_4 alignment: A_t = alignment/correction capacity
```

They project to separate namespace objects rather than a repository-global symbol table.

Therefore:

\[
\boxed{
A_t^{(action)}
\neq
A_t^{(alignment)}
\neq
A^{(adaptation)}
}
\]

unless some future adjudication independently establishes a relation.

```text
TOKEN_IDENTITY_AS_GLOBAL_SYMBOL_IDENTITY    = CONTAINED
INTRA_PAGE_SYMBOL_OVERLOAD_AS_SINGLE_MEANING = CONTAINED
LATEST_DEFINITION_WINS                       = CONTAINED
LOCAL_NAMESPACE_LEAKAGE_ACROSS_SECTIONS      = CONTAINED
```

The same rule applies to C/C_t, R/R_t, E/E_t, and Omega.

## 4. Symbol overload is not itself contradiction adjudication

The source is internally overloaded and in places formally awkward. For example PAGE_4 defines A_t as available action space, then uses A_t as the output of an action-selection equation, and later explicitly reuses A_t for alignment/correction capacity.

The compression preserves the source-local definitions and equations. It does **not** decide whether the first two PAGE_4 uses are compatible, erroneous, shorthand, or distinct latent objects.

\[
\boxed{
\text{overloaded notation}
\neq
\text{adjudicated contradiction}.
}
\]

```text
LOCAL_SYMBOL_OVERLOAD_AS_AUTOMATIC_CONTRADICTION = CONTAINED
```

The node can remember a formal tension before possessing an adjudicator.

## 5. Alignment forms remain variants, not one theorem

R15 gives multiple stability/alignment expressions:

```text
Delta Omega_t <= C_t

dOmega/dt <= kappa C_t

A_t >= C_t

Correction capacity >= Capability growth
```

The first family uses PAGE_00 C=correction capacity. PAGE_1/PAGE_4 use A=alignment/correction and C=capability/capability growth. PAGE_5 is verbal.

The compression therefore keeps a secondary `ALIGNMENT_FORM_VARIANTS` view with:

```text
FORMAL_EQUIVALENCE = NOT_ESTABLISHED
AUTHORITY_EFFECT   = NONE
```

```text
ALIGNMENT_FORMS_AS_PROVED_EQUIVALENT = CONTAINED
```

R15 strengthens the prior invariant:

\[
\boxed{
\text{same intended constraint family}
\not\Rightarrow
\text{same formal object}.
}
\]

## 6. Omega remains source-local

Omega is variously described as:

```text
controllable reachability
set of possible future states
future environment states reachable from S_t
reachable future space
```

The effective compression preserves four primary definition objects/namespaces and one zero-authority variant view.

```text
OMEGA_VARIANTS_AS_ONE_CANONICAL_SET = CONTAINED
```

The node does not decide whether these are strict synonyms, nested sets, approximations, or evolving formulations.

## 7. Loop recurrence does not canonicalize architecture

R15 contains several loop/chain objects:

```text
PAGE_00: R -> Omega -> C -> A -> R
PAGE_1: Reality/Observation/.../Feedback loop + R->M->A->E->R
PAGE_2: six-step unifying process
PAGE_3: diagrammatic self-improving loop
PAGE_4: state-transition/equation loop
PAGE_5: final-synthesis loop
```

They share themes but not identical node sets or semantics.

```text
LOOP_VARIANTS_AS_ONE_FORMAL_OBJECT        = CONTAINED
THEMATIC_SIMILARITY_AS_FORMAL_EQUIVALENCE = CONTAINED
```

The `LOOP_VARIANTS` secondary view explicitly withholds semantic identity and warrant independence.

## 8. Source synthesis does not become consolidation synthesis

PAGE_2 calls AIT a unified framework and says all components describe facets of one adaptive process.

PAGE_5 is source-labeled `FINAL SYNTHESIS`.

The node preserves both while keeping:

```text
PROGRAM_SYNTHESIS = NOT_ESTABLISHED
SUPERSESSION      = NOT_ESTABLISHED
CANONICAL_PAGE    = NONE
```

```text
SOURCE_UNIFIED_FRAMEWORK_AS_PROGRAM_SYNTHESIS = CONTAINED
SOURCE_FINAL_SYNTHESIS_AS_SUPERSESSION        = CONTAINED
PAGE_NUMBER_AS_AUTHORITY_ORDER                = CONTAINED
```

File numbering is source organization, not an authority ordering over propositions.

## 9. The AIT Stack does not become program topology

PAGE_3's vertical stack is retained as a source architecture/diagram:

```text
Values / Alignment
Feedback / Correction
Causality / Influence
Intelligence / Search & Planning
Representation / Compression & Abstraction
```

The compression emits no DEPENDS_ON, DERIVED_FROM, SUPPORTS, temporal, or causal Program Map edge from the diagram order.

```text
AIT_STACK_ORDER_AS_PROGRAM_DEPENDENCY = CONTAINED
```

## 10. Prior-framework references remain source-relative

R15 names multiple earlier framework labels and gives source-side glosses.

The compression preserves three reference registries:

```text
PAGE_00 related frameworks
PAGE_2 framework registry
PAGE_5 final-synthesis registry
```

with:

```text
EXACT_ENDPOINT_IDENTITY = UNRESOLVED
MAP_EDGE_EFFECT         = NONE
```

Therefore:

```text
SOURCE_REFERENCE_AS_RESOLVED_ENDPOINT                  = CONTAINED
SOURCE_REFERENCE_AS_PROGRAM_MAP_EDGE                   = CONTAINED
SOURCE_DESCRIPTION_OF_PRIOR_FRAMEWORK_AS_IMPORTED_TRUTH= CONTAINED
```

The later relation-extraction layer may revisit these source assertions. The compression layer may not resolve them itself.

## 11. R15 cannot repair R10's CGET identity

PAGE_2 expands CGET as `Causal Generative Equilibrium Theory` and PAGE_5 uses the acronym again.

R10 previously preserved multiple source-local expansions.

R15 therefore contributes another source occurrence only.

\[
\boxed{
\text{R15 lexical choice}
\neq
\text{R10 canonicalization authority}.
}
\]

```text
CGET_EQUILIBRIUM_LABEL_AS_R10_CANONICALIZATION = CONTAINED
```

## 12. Source labels do not promote standing

PAGE_4 names `invariant-preserving compression`; PAGE_5 says `FINAL SYNTHESIS`; multiple pages describe `core`, `central`, `defining`, or `unifying` principles.

These labels remain source characterizations.

```text
SOURCE_INVARIANT_LABEL_AS_THEOREM = CONTAINED
SOURCE_HYPOTHESIS_AS_ESTABLISHED_LAW = CONTAINED
```

Likewise PAGE_4's Q_R, H(S), CM(S), and CRM(S) formulas are source definitions/formalisms. No empirical validation is reported.

```text
FORMAL_DEFINITION_AS_VALIDATED_METRIC = CONTAINED
```

## 13. Proposed experiment remains prospective

PAGE_00 specifies a minimal experimental program, but the frozen head contains only six Markdown research texts.

There is no executable implementation and no execution record.

```text
PROPOSED_EXPERIMENT_AS_EXECUTION = CONTAINED
PREDICTION_AS_REPORTED_RESULT    = CONTAINED
```

The effective bounded absences are:

```text
NO_REPORTED_EMPIRICAL_RESULT
NO_REPORTED_NEGATIVE_RESULT
NO_EXECUTABLE_IMPLEMENTATION
NO_EXECUTION_RECORD
```

## 14. Recurrence and secondary views do not multiply warrant

AIT's adaptive loop, directional-expansion objective, correction/capability relation, and synthesis thesis recur across files.

Every source occurrence remains reconstructible through the primary ledger, but recurrence supplies no independence result.

```text
WARRANT_INDEPENDENCE_STATUS = NOT_ESTABLISHED
WARRANT_MULTIPLICITY_EFFECT = NONE
```

Secondary collision/variant/reference views have:

```text
AUTHORITY_EFFECT = NONE
```

Thus:

```text
RECURRENCE_AS_CORROBORATION      = CONTAINED
SECONDARY_VIEW_AS_SECOND_WARRANT = CONTAINED
```

## 15. Bounded absences are derived, not authored

No sentence is fabricated in which R15 says it has no results, implementation, or execution record.

Those bounded absence objects derive from the complete six-path source surface plus complete parse accounting.

```text
DERIVED_BOUNDED_ABSENCE_AS_SOURCE_ASSERTION = CONTAINED
```

## 16. Internal arrows remain internal

R15 contains many arrows in source formalisms and diagrams. None is a cross-repository edge.

```text
INTERNAL_ARROW_AS_PROGRAM_MAP_EDGE = CONTAINED
MAP_EDGE_EMISSION                  = NONE
```

## 17. Transportability result

R15 required two pre-projection conformance overlays:

```text
CONFORMANCE 001:
  separate Gamma definition from interpretations
  separate PAGE_4 possibility definition from formal partition
  separate PAGE_4 diagrammatic flow from formal equations
  separate PAGE_5 final-goal hypothesis from conceptual synthesis

CONFORMANCE 002:
  preserve PAGE_2 Delta Omega>0 as formal recurrence
  preserve PAGE_2 conceptual component claims separately from source interpretations
```

All use distinctions and coordinates already earned by the effective R01-R14 contract.

No post-projection repair is required.

```text
NEW_EPISTEMIC_DISTINCTION_REQUIRED = NO
NEW_GLOBAL_PARSER_ROLE              = NONE
NEW_TOP_LEVEL_COMPRESSION_COORDINATE= NONE
POST_PROJECTION_COMPRESSION_REPAIR  = NONE
AMENDMENT_005                       = NOT_EARNED
```

Bounded result:

```text
EFFECTIVE_COMPRESSION_CONTRACT_TRANSPORT = SUPPORTED_ON_R01_R15_FROZEN_HEADS
```

This is not a universal transportability claim.

## 18. R15 verdict

```text
R15_SOURCE_SURFACE                  = FROZEN_FULL_RECURSIVE_HEAD
R15_TOTAL_BLOB_PATHS                = 6
R15_RESEARCH_BEARING_PATHS          = 6
R15_UNIQUE_HEAD_BLOBS               = 6

R15_EFFECTIVE_PRIMARY_PARSE_UNITS   = 189
R15_DERIVED_AUDIT_VIEWS             = 5
R15_UNRESOLVED_SOURCE_FRAGMENTS     = 0
R15_PARSER_FAILURES                 = 0

R15_PRIMARY_PROJECTION_ENTRIES      = 189
R15_UNMAPPED_PARSE_UNITS            = 0
R15_DUPLICATE_PRIMARY_OWNERS        = 0
R15_COMPRESSION_SECONDARY_VIEWS     = 8

R15_REPORTED_EMPIRICAL_RESULT       = NONE_OBSERVED_ON_FROZEN_SURFACE
R15_REPORTED_NEGATIVE_RESULT        = NONE_OBSERVED_ON_FROZEN_SURFACE
R15_EXECUTABLE_IMPLEMENTATION       = NONE_OBSERVED_ON_FROZEN_SURFACE
R15_EXECUTION_RECORD                = NONE_OBSERVED_ON_FROZEN_SURFACE

R15_REUSABLE_NODE_STATE             = EARNED
R15_MAP_EDGE_EMISSION               = NONE
R15_MAP_AUTHORITY                   = NONE
R15_SCIENTIFIC_AUTHORITY            = NONE
PROPAGATE_KERNEL                    = NOT_EARNED
CEREBRO_STEP_2                      = CLOSED
AMENDMENT_005                       = NOT_EARNED
```

## 19. Sequential boundary

```text
R16_PROGRAM_PARSE_ACCESS = NEXT_AUTHORIZED_REPOSITORY
R17_R43_PROGRAM_PARSE_ACCESS = NOT_YET_OPENED
```

This is procedural authorization only and creates no R15 -> R16 semantic edge.

R15 therefore becomes a reusable neuron capable of remembering **local symbol namespaces and source-authored synthesis claims without treating either notation or rhetoric as self-resolving authority**.
